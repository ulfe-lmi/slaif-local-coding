# OAP Work Order — 005-al

## Objective, authority and exact state

Resolve the remaining acceptance-harness identity_replay validation blocker with
the smallest closed structural discriminator and any demonstrated harness-only
correction. Obtain decisive complete protected acceptance or precise first-failure
ownership evidence. AMEND_EXISTING_PR, Objective005; NO NEW PR, no new subsystem.

- Repository ulfe-lmi/slaif-local-coding; PR7:
  https://github.com/ulfe-lmi/slaif-local-coding/pull/7
- Base/head main / oap/005-gateway-ingress-integration.
- Required immutable start63f054b820773ca46164f3330806e92a7689a6e4;
  report-only parent2de3ff68b075fbd9f94d2353e430f95500c86137;
  tested005-ak source989e2c09ce95f1d93c9bfb24eaa7a79824da264c.
- Main570bd2b24ad4b041a07e0320d5ed44bc73e99ad5. PR OPEN/CLEAN and final
  test SUCCESS34486782217/job102902973002. Local/GitHub head and005-al
  availability independently reconciled. Preserve unrelated empty untracked
  Local, clean, unchanged; exclude from commits. Clean detached task worktree
  at the literal candidate is allowed; never delete/reset unrelated files.
- Gateway executable50dcc3b85d614eb1d0c6196595bf22ef5779f846;
  merged-main authorityd142fd7f04c46fac3469b9b9bba1bd2068aabad8;
  accepted reportad1c556a539130c08dc1063b3e9a25a0af2e5a85;
  app treec0204deaff3cfd055a25f29a7f5d8d3c5e161d57.
  Exact clean fixture, merged reachability and all ten accepted-head checks
  independently verified. Keep pins unchanged; no branch-name test authority.

Read governance, this order,005-ak report and its bounded current evidence.
The human accepts005-ak as immutable history: ordinary real Codex/tool and vision
full-image/resumed-crop transport succeeded,5/6 inference terminals validated,
all responses were below131072, first failure was stream_validation_invalid at
the identity_replay companion. This class alone proves no product/owner defect.
Do not rewrite/rerun005-ak, reopen the accepted frame repair or replay history.

## A. Minimal localization and correction

Inspect the exact initial companion path and existing closed validation sites
before any protected request. Strategic source review locates the failure at
the first streamed companion request in operation5, before returned-call capture
can authorize the ID-less continuation. Existing retained evidence does not
identify which semantic validation stage failed.

Check locally whether the companion's request/profile/fixture assumptions already
demonstrate a deterministic harness defect. In particular its current initial
request uses generic fixture text, declares tools, sets max_output_tokens32, and
does not explicitly select a function, while the test requires a returned function
call. Inspect this mismatch and the actual server-selected/profile/declaration
contracts; do not assume it caused005-ak or raise numeric limits to hide it.
Where existing source and bounded non-protected evidence prove a harness-only
defect, fix that exact defect here before Attempt1. No speculative redesign.

Add only the smallest closed discriminator to the existing observer/runner:
unknown/unaccepted event class; event-name/payload-type mismatch; exact Gateway
validator rejection; replay-candidate type/shape rejection; framing separately
where applicable; other closed validation stage. Include response-identity
relationship failure within an appropriate closed class, not raw identifiers.
Preserve the first observed stage through existing stop/projection/cleanup.

Only fixed subtype, permitted finite event/type class, existing phase/ordinal/
lifetime and bounded integer facts may be retained. Known event names must come
from the existing finite set; unknown names become a fixed other class. No event
bodies, excerpts, prompts, output, tool arguments, raw IDs/digests, signatures,
images, source content, credentials, arbitrary exception text or new log sink.
Use existing records and evidence projection; no new observer, diagnostic/evidence/
provenance framework, synthetic matrix variant, or acceptance predicate.

The companion MUST still prove the same intended ID-less call-ID/HMAC ownership
contract: actual initial streamed function call, canonical returned call-ID, actual
continuation omitting the optional item ID and retaining mandatory call-ID, same
owner/scope, security negatives and accounting. No hard-coded IDs/PASS, dropped
test, fabricated history, lenient substitute validator or weakened replay policy.
A legitimate fixture/request correction must preserve the actual contract.

## B. Qualification and protected attempt authority

After execution-relevant changes, run focused affected positive/negative tests,
ordinary lint/type and existing repository/CI/package/privacy gates, and the
existing exact real-Codex0.149 fake37/37 qualification on the frozen candidate.
No unrelated historical qualification or additional synthetic variant. Preserve
accepted integrated tests; do not weaken an existing guard to reuse stale evidence.
Verify the produced private fake file with the actual gate before credentials.
Use exact source/module hashes; metadata-only commits do not require another run.
Keep results outside the worktree until protected execution is finished.

Human explicitly authorizes AT MOST TWO real protected matrix attempts in005-al.
This is a bounded exception to the previous single-attempt order, not an unlimited
retry loop or permission to alter005-ak. Each attempt retains the existing per-run
numeric limits and internal zero-retry behavior. Preserve separate immutable safe
evidence/source/counts/cleanup for each attempted run; never replace Attempt1 with
Attempt2 or describe two attempts as one. No third protected run.

1. Attempt1: after the minimal discriminator/fix and qualification, run the
   existing complete protected matrix. If PASS, stop testing and return for
   strategic final acceptance. On its first failure, stop later protected
   dispatch within that attempt and finish exact owned cleanup.
2. Attempt2 is permitted ONLY if Attempt1's new direct evidence, confirmed by
   bounded source/non-protected reproduction, demonstrates a deterministic Local
   acceptance-harness-only defect. It must be correctable without production,
   Gateway/Qwen, budget, identity/replay/security-policy or credential changes.
   Fix it immediately within005-al; run focused non-protected qualification and
   whatever existing exact-source fake gate is necessary after the code change;
   then perform ONE FINAL complete protected attempt. No routine human suffix
   approval or new order is needed for this already-authorized conditional step.
3. If ownership points to Gateway, Local production, Codex, Qwen, or unresolved
   protocol/security semantics, do NOT run Attempt2. A generic validator rejection
   alone is not proof of harness ownership. Preserve direct failure and stop.
4. Attempt2, if used, is final. Failure stops the round. No005-am activation.

Local preflight repairs proven before any protected request may be completed in
this continuation with focused gates; never treat unknown traffic counts as zero.
No ad-hoc protected diagnostic calls, alternate prompts or hidden retries outside
the at-most-two complete matrix attempts.

## C. Existing acceptance and protected envelope

Run real unmodified Codex0.149 -> exact Gateway -> frozen Local candidate ->
unchanged existing vision Qwen. Require ALL existing protected C1-C5 obligations,
including the companion and remaining signing/replay/idempotency/tamper/tool/SSE/
isolation/accounting/privacy/cleanup checks, compiler/cache/dependency/rehydration,
readiness/models/nonstreaming, ordinary tool continuation and full-image/resumed
crop history. Fake D rollback/cutover-refusal predicates remain; actual cutover
is excluded. No new acceptance predicates or partial-to-full PASS projection.
Do not claim all-request final accounting/privacy from an earlier checkpoint.
Controlled failure uses the existing synthetic failure route, never Qwen damage.

Host hinton1 independently verified: user qwen-serving-vision.service active,
MainPID23961,UID1029,startSun2026-09-06 18:57:26CEST,NRestarts0,18020 listener;
text unit inactive/MainPID0 expected; seven pre-existing Qwen changed paths;
18021/18030/18031 free. Preserve existing qwen3.8-27b vision model/configuration,
context100000, one sequence/image and GPU/launch flags. Candidate127.0.0.1:18031;
Gateway verified-free ephemeral loopback port. Record actual ownership/cleanup.

Exact acceptance Codex binary:
/synology/homes/janezp/.codex/packages/standalone/releases/0.149.0-x86_64-unknown-linux-musl/bin/codex
SHA256 bbc3341e44c9ead340ed9570c17be936e37870f570751a941699ffd04d672827.
External coding profile remains oap-coding-luna-xhigh; it is not the test client.
One verified terminal wrapper was left waiting after strategic removed only its
own duplicate idle background reader. Do not start another coding worker.

Same UID/EUID1029. Use only existing read-only preflight/readiness and the already
approved exact active vision MainPID's unique nonempty VLLM_API_KEY acquisition
in bounded memory, after gates. Pass only through existing QWEN3090_API_KEY/Local
upstream header; discard; never print/hash/persist/argv/public metadata or broaden
credential/environment/config discovery. Private dirs0700/files0600, regular/
single-link/no-follow/owner guards unchanged. No whole-run sudo; existing bounded
sudo Docker/PostgreSQL16 loopback/tmpfs/--rm helper only. Coding owns routine setup.

Per attempt:131072-byte HTTP-response cap and accepted131072 frame cap unchanged;
900 seconds,64 observations, one active dispatch, nine existing macro operations
maximum1 each, existing compiler/inference/output ceilings, zero internal retries.
No budget increase, additional inference, service mutation or network expansion.

Allowed: smallest existing observer/runner/helper propagation and deterministic
harness-only correction, focused tests, directly affected docs/evidence/transcript.
Forbidden: Local src/, Gateway, Qwen/vLLM/model/service/configuration/patch/venv/
unit/launch/GPU/network/firewall/VPN/credential/profile mutations, dependencies/
locks/Codex versions, identity/replay security semantics, public binding,
persistent deployment, release publication, or unrelated work. Prior artifacts
and unrelated files remain untouched.

## D. Immutable closure and strategic merge

Publish ONE report with exact source/Local/Gateway/Codex/Qwen authority, minimal
localization/correction evidence, focused/fake results, actual attempt count and
per-attempt first failure/ownership/accounting/privacy/cleanup limitations. If
Attempt2 ran, state its demonstrated eligibility and preserve Attempt1 evidence.
Distinguish IMPLEMENTED, TESTED, REAL-E2E ACCEPTED, CUTOVER ACCEPTED, MERGED and
RELEASE-READY. No unsupported product-defect or readiness claim.

Amend PR7 only. Push all non-report work, record literal implementationSHA,
atomically publish exactly
oap/reports/005-al-identity-replay-validation-and-closure.md with
Implementation head SHA and Report publication commit: SELF. Final commit only
that report; parent exactly implementationSHA. Verify remote head/parent/path/
bytes and current check states, then send exact response FIFO OK. Coding never
merges or enables auto-merge; no later mutation after its report publication.

Full real PASS returns directly for strategic final review and authorized PR7
merge if every existing OAP criterion/final-head check passes. No extra proof
continuation. If permitted attempts fail or ownership is unresolved, publish
precise safe first failure and STOP; do not automatically generate005-am. Even
after PASS/merge, no persistent deployment/cutover/release; strategic reports
the exact tested stack and minimal remaining operational steps for human use.
