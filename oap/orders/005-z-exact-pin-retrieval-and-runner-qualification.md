# OAP Work Order — 005-z

## Objective

Complete the missing 005-y repository-only qualification and correct its
directly reproduced operation-permission defects. Retrieve the already
authorized exact Gateway ancestor correctly; no human terminal work or new
access authorization is needed. This is an ordinary continuation of Objective005
on PR7, not a new feature or Gateway investigation. Protected Qwen traffic,
credentials and installed cutover remain prohibited in this round.

## Exact GitHub state and immutable authorities

- AMEND_EXISTING_PR; NO NEW PR. Objective005, round005-z.
- Repository ulfe-lmi/slaif-local-coding, PR7:
  https://github.com/ulfe-lmi/slaif-local-coding/pull/7
- Base/head: main / oap/005-gateway-ingress-integration.
- Main: `570bd2b24ad4b041a07e0320d5ed44bc73e99ad5`.
- Required starting immutable005-y report:
  `d56be56d01ccb070594df941810fb2a77b334d33`.
- Its literal implementation parent:
  `be8428857b26bc032324b7d9610e23baa458a670`.
- Exact report: oap/reports/005-y-operation-permits-and-protected-branch-conformance.md.
- Strategic verification: report-only parent/path; exact order bytes; clean
  checkout; PR open/non-draft/CLEAN and only open PR; final report-head test
  SUCCESS34411150677/job102665610824. Suffix005-z unused.
- Gateway executable/non-report authority:
  `50dcc3b85d614eb1d0c6196595bf22ef5779f846`.
- Gateway merged-main authority:
  `d142fd7f04c46fac3469b9b9bba1bd2068aabad8`.
- Gateway report: `ad1c556a539130c08dc1063b3e9a25a0af2e5a85`;
  app tree: `c0204deaff3cfd055a25f29a7f5d8d3c5e161d57`.
- Production introduction: `899bd57e6eef49149d54c65838cf25d043e34a55`.
- Local PR7 handback comment5601188832 reconciled directly with GitHub.

Reconcile remote state before edits. After activation only exact order and
active=005-z are expected differences. Preserve all earlier orders/reports,
especially immutable005-q and UNKNOWN005-r/v facts. Do not rerun, rewrite or
reinterpret historical rounds. New evidence is only this continuation.

## A. Exact dependency retrieval is routine coding setup

Independent authenticated GitHub reads establish that 50dcc exists. Compare
50dcc...d142 returns ahead_by2, behind_by0; app tree matches exactly. Gateway
report is report-only child of50dcc; all ten final report-head checks successful.
The 005-y claim that the pin is unavailable because it is not advertised as a
ref is unsupported. A non-tip commit need not appear in git ls-remote.

Use existing authenticated Git access to retrieve merged-main ancestry into
a task-owned checkout, then detach at the exact executable50dcc commit. Fetch
merged main with complete ancestry, or deepen a task-owned shallow clone enough
to include its report parent and implementation parent. Do not replace the
acceptance pin with main, another revision, a branch name or an archive missing
required Git ancestry. Verify rev-parse HEAD, merge-base --is-ancestor into exact
d142, app tree and imported source identity before execution. Reconcile report
and final checks directly. Preserve the existing Gateway workspace: disposable
checkout only, no Gateway product commits, PR changes or protected mutations.

If an actual fetch fails, investigate bounded safe alternatives and report its
sanitized exit/error class and the exact object/ancestry test. Empty advertised
SHA search alone is not a retrieval failure. Do not request routine authority
already granted by this order or make the human/strategy your terminal operator.

## B. Bind dispatch to the current operation, not any same-phase permission

Independent no-network reproduction on005-y:

1. Admit codex_turn_1 at codex/ordinal1; dispatch inference with ordinal2:
   permitted and silently recorded under ordinal1.
2. Pre-admit turns1/2; consume compiler2+inference1; request inference while
   context remains ordinal1: permitted from turn2's pending allowance.
3. PUBLIC_DISPATCH_PLAN totals compiler6/inference14/other5=25, although
   report/docs claim compiler6/inference12/other5=23.

Correct the existing plan/controller/callers. Every actual API dispatch must
consume the exact current operation/phase/kind/ordinal/lifetime slot. Reject
wrong ordinal, missing operation, exhausted slot, expired run, wrong kind,
stale lifetime and a request trying to borrow another operation's permission
before delegate invocation. Do not silently rewrite attribution or advance to
another operation solely because an allowance ran out. Logical operations and
individual provider requests are distinct; map the existing legitimate tool
roundtrip and vision requests explicitly. Demonstrate the real transition that
authorizes a next turn/request; pre-admitting an entire subprocess is not proof.

Derive finite request mapping from the actual existing harness sequence, with
safe phase/operation/ordinal/kind counts. Reconcile maxima versus measurements
honestly; do not label a25-slot allowance a23-slot measurement. No increase to
existing authorized inference/compiler/other bounds or retry counts to make
tests pass. Existing aggregate64 observations is a ceiling, not permission for
extra inference. Keep900 seconds, nine macro operations maximum1/zero retries,
compiler/output limits,16-KiB event,128-KiB stream and single concurrency.
If mapping needs a new resource allowance, stop before protected traffic and
state the exact gap; this round uses fake resources only.

Regressions through connected observer with delegate counters must cover both
reproductions, real allowed sequences, extra Codex/provider turn, extra compiler
retry, same-phase wrong ordinal, expiry between turns, old observer/new lifetime,
phase transition and queued dispatch after failure. Re-establish prior frame/
chunk, close/cancellation and semantic terminal-valid regressions.

## C. Execute the actual protected-mode branch safely against synthetic dependencies

Complete005-y criterionC, not just add hooks. Use the existing shared runner,
ProtectedRuntimeHooks and actual unchanged Gateway/Local modules, with physical
provider a disposable loopback fake. Guard real /proc/environ, credential source,
18020 and external protected access: accidental access must fail the test.
Keep protected_acceptance=false and explicit synthetic provenance.

Execute separately and serially, with clean task-owned lifetimes:

1. Healthy protected-mode synthetic run past preflight, candidate startup,
   actual dispatch/response/terminal paths and every selected protected row.
2. Known actual dispatch followed by injected observer failure; zero later
   dispatch and exact preserved attempted/dispatched/responded/completed/
   terminal-valid counts per compiler/inference kind and observer lifetime.
3. Primary failure from2 plus projection and cleanup failures; preserve primary
   failure, counts, mode and bounded secondary classes through serialization.
4. Missing mapping/dependency before dispatch; complete selected dispositions,
   zero credential-hook/provider-network calls.

Assertions must prove the actual shared implementation, candidate and dispatch
callbacks ran in1–3. Preflight-only29-row serialization is not healthy execution.
Do not recursively start a second candidate on18031 while the outer fake runner
still owns that port:005-y removed the nested preflight stop after the outer
candidate had already started. Use separate serial lifetimes and actual cleanup.
No dummy PASS predicates or synthetic-only replacement acceptance algorithm.

Inspect and correct two directly visible evidence/cleanup issues within the
existing harness: candidate_ready is assigned200 although candidate builder
only probes /healthz and observer capability; measure the actual readiness
predicate with explicitly bounded request mapping, or truthfully mark missing.
Also dispatch_complete_hook can raise after delegate response exists but before
responded is recorded/stream wrapped. Close that returned stream exactly once,
preserve actual response counts and release resources; qualify with an injected
exception and cancellation, not only a hook that returns normally.

If complete synthetic execution reveals another direct Local runner/predicate
defect, repair it within this order's repository-only boundary, then requalify
the affected and full suites. Do not stop at an already authorized routine
setup issue; do not declare conformance from unexecuted helper code.

## D. Complete exact fake matrix and repository gates

After repairs run the full existing real-Codex0.149 fake C1–C5/D matrix: all37
selected rows and projections, same-session full-image→crop/history, exact
Gateway/Local admission, compiler/cache/rehydration, signing/identity, isolation,
replay/idempotency/tamper, tool lifecycles/call-ID-HMAC ownership, authorization,
controlled failure, reservation/ledger finalization and zero pending state,
privacy, cleanup and fake rollback/cutover refusal. Re-establish them freshly.
Do not substitute Gateway161 direct fake evidence or earlier Local fake evidence.

Codex fixture binary exactly0.149.0, checksum
bbc3341e44c9ead340ed9570c17be936e37870f570751a941699ffd04d672827,
at /synology/homes/janezp/.codex/packages/standalone/releases/0.149.0-x86_64-unknown-linux-musl/bin/codex.
Keep client and active profiles unmodified. Confirm exact Local tested SHA,
clean source/import identity and unchanged pin; no code edits after claimed
qualification without rerunning affected qualification. Capture separate fake
and synthetic-protected results, phase traces, exact counts and cleanup facts.

Run focused regressions and frozen full pytest/Ruff/format/mypy/compileall,
shell syntax, build/wheel boundary, diff/pin/privacy checks and required CI.
Update current docs to measured guarantees only; correct the misleading permit
totals and unexecuted conformance claims without altering immutable reports.

## Protected-host, security and scope boundary

Allowed edits: existing tests/helpers, rehearsal script, relevant tests/current
docs, exact order/active and report. No src/, Gateway implementation, dependency/
lockfile/version/binary, new subsystem/relay, installed service or cutover.

Read-only host baseline verified: hinton1 vision unit active/running PID23961,
start Sun2026-09-06 18:57:26 CEST, NRestarts0,18020 listener and health200.
Text unit inactive/MainPID0 is EXPECTED, not a discrepancy. Seven unrelated
Qwen worktree entries;18021/18030/18031 free. Protected model qwen3.8-27b,
context100000, one sequence/image; existing coding profiles remain unchanged.
Reconcile actual OAP executor profile/provider safely before work.

Only read-only unit/PID/start/restart/listener and unauthenticated health status
allowed on protected service. No protected credential/environment/config/log
read, authenticated/model/compiler/inference/vision/diagnostic request, restart,
instrumentation, Qwen/model/checkpoint/patch/venv/unit/launch/GPU/key/network/
firewall/VPN/profile/18020 mutation. No second model. This order deliberately
qualifies the protected runner without invoking protected Qwen.

Coding owns routine task-local setup/tests/publication. Existing task-owned
PostgreSQL16 loopback/tmpfs/--rm and bounded Docker setup allowed; no host daemon,
package or privileged-network changes. Use private disposable client/cache state;
clean exact owned resources/listeners and preserve unrelated state. No raw
prompts/source/images/SSE/tool/model content, credentials/signatures/nonces/IDs,
private endpoints or arbitrary errors in evidence. Count and classify only.

## Immutable report and return

Amend only PR7. Push intended non-report changes with exact order/active, inspect
required checks, record literal implementationSHA. Atomically publish exactly
oap/reports/005-z-exact-pin-retrieval-and-runner-qualification.md containing
Implementation head SHA and Report publication commit: SELF. Final commit
changes only that report, parent=literal implementationSHA. Verify remote exact
head/parent/path/bytes before response FIFO OK. No coding merge/auto-merge.

COMPLETE requires allA–D, otherwise truthful PARTIAL/FAILED with first direct
gap, exact tests, phase/count/budget/source/accounting/cleanup evidence and
limitations. Distinguish IMPLEMENTED, TESTED, REAL-E2E ACCEPTED, CUTOVER ACCEPTED,
MERGED and RELEASE-READY. Fake and synthetic protected tests do not establish
protected acceptance. Real protected matrix, cutover, merge and release remain
NOT RUN/unaccepted here. Return immutable evidence for strategic review.
