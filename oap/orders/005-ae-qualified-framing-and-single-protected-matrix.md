# OAP Work Order — 005-ae

## Objective and exact state

Close the directly evidenced Local SSE framing and exact-evidence gaps, run fresh
full fake qualification on the frozen source, and only if every gate below passes
execute exactly one bounded protected Objective005 matrix. Preserve the actual
omission companion and all existing controls. This is AMEND_EXISTING_PR on PR7,
NO NEW PR or feature objective. Coding never accepts, merges or enables auto-merge.

- Repository ulfe-lmi/slaif-local-coding; PR:
  https://github.com/ulfe-lmi/slaif-local-coding/pull/7
- Base/head main / oap/005-gateway-ingress-integration.
- Main `570bd2b24ad4b041a07e0320d5ed44bc73e99ad5`.
- Required immutable start report `42f302961e0f651c8091e6949c1729c212cb2eda`;
  report-only parent `302f11aaaaa4427f469a6cb0a0d5e14d88434bfe`.
- Report oap/reports/005-ad-prove-actual-idless-matrix-continuation.md.
- Strategic verified exact order/report topology, clean checkout, open/non-draft/
  CLEAN PR, final test SUCCESS34435157213/job102738569536;005-ae unused.
- Gateway executable `50dcc3b85d614eb1d0c6196595bf22ef5779f846`;
  merged-main authority `d142fd7f04c46fac3469b9b9bba1bd2068aabad8`;
  report `ad1c556a539130c08dc1063b3e9a25a0af2e5a85`;
  app tree `c0204deaff3cfd055a25f29a7f5d8d3c5e161d57`.
  Ancestry/app and all10 final Gateway checks independently reconfirmed.

Reconcile GitHub, imports/source identity, exact pins and host before mutation.
After activation only exact order and active005-ae are expected new differences.
Keep every previous order/report/evidence immutable, particularly005-q and the
UNKNOWN005-r/v facts. Do not rerun or reinterpret earlier rounds. Gateway161
handback remains authority, not a substitute for Local protected acceptance.

## A. No-protected-access preparation and qualification

### Provider SSE framing

The direct observer still requires an explicit event: header, although the exact
Gateway parser/validator accepts data-only typed SSE. Strategic reproduced nine
valid complete message-lifecycle events(2046 bytes): Gateway accepts all, Local
observer fails framing. The public helper supports this representation already.

Repair the existing provider observer to handle the same accepted typed Responses
events with explicit event headers, data-only frames, and mixed representations,
without altering bytes or weakening the injected exact Gateway validator. When
an explicit event header exists it must agree with the payload type. Missing/
unknown type, malformed JSON, conflicting header/type, invalid ordering or usage
must still fail. Use Gateway's actual parser/validator and terminal rules as the
oracle, not an always-accepting test stub. Preserve comment handling, terminality,
bounded incremental parsing, cancellation, close-once, duplicate/early terminal
rejection,16KiB frame/128KiB stream limits and bounded distinct categories.
Test split/coalesced/LF/CRLF representations, actual message/function/reasoning
lifecycles, and data-only terminal frames. Do not synthesize a terminal PASS.

### Exact generated evidence

005-ad qualification declares37 fake rows but lists38, including protected-only
C5.4. Its runner hash belongs to11aef87, not stated tested sourcec5bdcf; only a
type annotation differs, but the exact attestation is wrong. Preserve that file.

Produce future sanitized evidence directly from the actual runner results and
actual frozen source. Validate row IDs/cardinality/statuses against each mode's
closed manifest(37 fake,29 protected), hashes against the stated Git source and
loaded modules, and actual CI/run identity. No hand-written all-PASS lists or
manual replacement of failed/missing predicates. Retain per-row results, exact
per-kind/per-lifetime counts, companion witness, accounting and cleanup. Include
concrete command parameter bindings and SHA256s; no raw captures/IDs/credentials.
The evidence validator must reject stale source/hash, extra/missing rows and
inconsistent counts before protected credential access. Keep the existing strict
same-source fake-result gate; do not weaken it to accommodate a stale artifact.

Retain actual composed omission with same-call matching/terminal/accounting and
separate natural Codex present-ID facts. Re-establish missing/mismatched call-ID
and wrong-key ownership denial at the current composed boundary under existing
negative-matrix scope, before provider advancement, with zero pending state.
Do not assign HMAC/no-scope-downgrade merely from a generic success flag; show the
exact signed route and observed accepted/rejected relationships without raw IDs.
Any inability to provide required evidence within existing bounds means NOT RUN
for protected traffic and a truthful report, not a relaxed gate.

### Freeze and pass before any protected credential/traffic

Run all focused framing/provenance/ownership negatives and frozen full pytest,
Ruff/format/mypy/compileall/shell/build/wheel-boundary/diff/pin/privacy checks.
Push all implementation plus exact order/active, inspect/fix in-scope CI, freeze
the literal clean tested code SHA. Every required implementation-head check must
be present and successful before protected work; none pending/failed/cancelled.

On that exact source execute the full existing real-Codex0149 fake37/37 C1–C5/D
matrix and shared protected-mode synthetic29/29 with semantic fake oracle
unavailable. Execute the four serial healthy/observer-failure/combined-failure/
zero-access mapping cases. Verify first-failure stop, exact preserved counters,
all row dispositions and cleanup. No missing predicate or unqualified framing/
factory/source condition may be discovered by spending protected inference.

Keep generated safe results outside the checkout until testing is complete so
the tested source remains clean. Any relevant source/configuration change
invalidates qualification and requires fresh fake tests and CI. A later report/
evidence-only descendant must be explicitly attested, never silently substituted.

## B. Protected authority and immutable live baseline

Standing human delegation authorizes one bounded protected matrix after A is
entirely satisfied. This is a newly reviewed attempt after Local observer fixes,
not a retry loop. It does not authorize protected service or configuration mutation.

Host hinton1 baseline: qwen-serving-vision.service active/running, MainPID23961,
start Sun2026-09-06 18:57:26CEST, NRestarts0,18020listener/health200. Text unit
inactive/MainPID0 is expected. Model qwen3.8-27b/context100000/one sequence/image,
seven pre-existing Qwen worktree changes. Ports18021/18030/18031 free at review.
Discover/reconcile the actual OAP executor profile/provider before work and keep
it unchanged. Candidate must bind127.0.0.1:18031. Existing task Gateway may use
the harness's verified-free ephemeral127.0.0.1 port; record its actual PID/port
for cleanup. Do not infer cleanup from18030 alone. No public interface expansion.

Only after full A PASS and unchanged protected preflight: resolve MainPID of the
exact active vision unit, require unchanged identity, retrieve only its unique
nonempty VLLM_API_KEY entry into bounded memory, pass only through the task
harness QWEN3090_API_KEY/in-memory Local upstream header, then discard. This is
the existing005-o/r/v credential scope. Never print, hash, persist or put it in
argv; never pass it to Codex, public Gateway metadata or artifacts; do not source
Qwen config or inspect unrelated environment entries. No key access before A.

Never mutate Qwen/model/checkpoint/patch/venv/unit/launch/GPU flags/key files,
firewall/VPN/network bindings or active Codex profiles. No instrumentation,
service switch/restart, second model, alternate provider, model/version or
credential expansion. No Gateway implementation or PR change. An unavailable
fixture is NOT RUN/BLOCKED, not permission to repair it.

## C. Exactly one protected matrix, only after A+B

Use real unmodified Codex0.149.0 -> exact Gateway50dcc -> frozen Local candidate
with qualified direct observer -> unchanged protected vision Qwen. Exact Codex:
/synology/homes/janezp/.codex/packages/standalone/releases/0.149.0-x86_64-unknown-linux-musl/bin/codex;
SHA256 bbc3341e44c9ead340ed9570c17be936e37870f570751a941699ffd04d672827.

Keep the current finite plan:900 seconds, aggregate64, nine macro operations
maximum1/zero retries, existing per-operation compiler/inference/output limits,
one stream/compiler concurrency,16KiB/frame and128KiB/stream. Health/model and
readiness probes are explicit, counted and method/endpoint scoped. No extra
diagnostics, prompts, calls, retries, inference or byte-budget increases.

Require every selected protected C1–C5 obligation, not merely the companion:
readiness/authenticated model visibility/ordinary nonstreaming; real Codex
two-turn tool roundtrip; reasoning/function/message SSE and terminal usage;
same-session reuse and zero-root rehydration; compiler/cache/governance/dependency
acquisition; independent session/key/owner/repository isolation; full-image then
actual same-session resumed crop/history with newest-only upstream image and
both terminal/governance results; signed identity every admission; actual ID-less
companion through Gateway HMAC, separately from natural present-ID Codex shape;
replay/idempotency/tamper/unauthorized/hosted/quota negatives; per-request ledger/
reservation finalization or release, correct usage/counters, zero pending or
duplicates, compiler exclusion; privacy/no-bypass and exact cleanup/preservation.

Controlled provider failure remains the existing disposable synthetic failure
route, never deliberate Qwen failure. At FIRST failed, unobserved or unavailable
protected boundary, latch stop before every later protected dispatch. Preserve
actual attempted/dispatched/responded/completed/terminal-valid counters, request
phase/ordinal and source identity, original primary plus secondary failures,
accounting/release and cleanup. No protected retry after repair or alternate
diagnostic call. Do not patch Gateway or Qwen; establish ownership from bounded
direct evidence and return for strategic review. Local support corrections after
the stop may be qualified fake-only, never used to erase/re-run the attempt.

## D. Cleanup, evidence, publication and final decision boundary

Prove cleanup of actual task Gateway/Local/PostgreSQL/client/cache/listener
resources and unchanged protected PID/start/restarts/config/model/key/profile/
network state using permitted safe facts. Existing PostgreSQL16 loopback/tmpfs/
--rm and bounded Docker setup only; no host packages/daemon or privileged network.
Use exact validated task-owned targets, preserve unrelated work. No raw prompts/
source/images/tool/model/SSE bodies, keys, IDs, signatures/nonces/private endpoints
or arbitrary errors in logs/cache/evidence/report. Only bounded generated safe
facts. Coding owns routine setup, implementation, tests and publication.

Allowed implementation edits are the existing observer/rehearsal/tests/current
docs/generated evidence needed for A, not src/, Gateway, product dependencies/
lockfiles, new relay/subsystem or installed service. Preserve prior artifacts.
Publish generated source-bound fake and protected results(or exact NOT RUN
reason), actual counts/first stop/accounting/cleanup/host evidence and limitations
under oap/evidence/005-ae. Current docs must match measured outcomes.

Actual installed cutover is NOT authorized by this round, even if protected
acceptance passes. Fake D proves refusal/rollback planning only. CUTOVER ACCEPTED,
MERGED and RELEASE-READY remain NO. Stop for strategic review before a separate
operational/cutover decision. Never merge or enable auto-merge.

Amend PR7 only. Push all non-report implementation/evidence plus exact order and
active; record literal final implementationSHA, then atomically publish exactly
oap/reports/005-ae-qualified-framing-and-single-protected-matrix.md containing
Implementation head SHA and Report publication commit: SELF. Final commit only
that report, first parent exactly literal implementationSHA. Verify remote head,
parent/path/bytes before exact response FIFO OK. Distinguish IMPLEMENTED, TESTED,
REAL-E2E ACCEPTED, CUTOVER ACCEPTED, MERGED, RELEASE-READY. COMPLETE only when
all ordered predicates genuinely pass; otherwise truthful PARTIAL/FAILED/BLOCKED
with immutable bounded evidence. Return for strategic review, not self-acceptance.
