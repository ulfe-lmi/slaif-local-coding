# OAP Work Order — 005-ac

## Objective and exact authority

Correct four directly reproduced Local acceptance defects left by005-ab.
Preserve its useful observed counters, zero-access preflight case and immutable
evidence. Same Objective005, AMEND_EXISTING_PR; NO NEW PR or feature objective.
No protected Qwen inference, credentials or service changes in this round.

- ulfe-lmi/slaif-local-coding PR7:
  https://github.com/ulfe-lmi/slaif-local-coding/pull/7
- Base/head main / oap/005-gateway-ingress-integration.
- Main `570bd2b24ad4b041a07e0320d5ed44bc73e99ad5`.
- Start immutable report `3658177c6669b673f5bab0f60d2eff08c4bcfae6`;
  report-only parent `c820a8f752eb49c72658eda60eaaa93bd2a61579`.
- Exact report oap/reports/005-ab-shared-protected-observation-path.md.
- PR open/non-draft/CLEAN, exact order/report topology and clean checkout
  verified; final test SUCCESS34425040384/job102708376958;005-ac unused.
- Gateway execute `50dcc3b85d614eb1d0c6196595bf22ef5779f846`; main authority
  `d142fd7f04c46fac3469b9b9bba1bd2068aabad8`; report
  `ad1c556a539130c08dc1063b3e9a25a0af2e5a85`; app tree
  `c0204deaff3cfd055a25f29a7f5d8d3c5e161d57`. Ancestry and10 final checks
  reconfirmed. Keep all pins, product modules and Gateway workspace unchanged.

Reconcile GitHub and import/source identity before edits. Only exact order and
active005-ac are expected new differences. All old orders/reports/evidence stay
immutable, especially005-q and UNKNOWN005-r/v facts. Coding owns safe setup.

## A. Remove the two remaining hook-selected real-path differences

At005-ab, the new counted health/models preflight executes only in the
protected_hooks branch; real protected takes the old direct unbudgeted branch.
Final protected provider_observation extracts the semantic child only with
hooks, but wraps the entire transport observation without hooks.

For EVERY protected-mode execution, use the same counted preflight function and
the same final semantic projection from the direct observer. Hook presence may
select physical provider/credential/host/clock or explicit failure injection;
it must not select another request-control, observation or acceptance path.
Delete the obsolete real-path duplicates, not merely repair the synthetic side.
Tests must prove hook presence/absence cannot choose different acceptance facts,
without touching real protected resources. Check first failed health/observer
boundary prevents later model/provider dispatch. Count health/models/readiness
and all observer lifetimes under the same existing deadline, stop and64 ceiling.

## B. Observe real call-ID relationships and actual ID omission

The classifier currently calls _FakeQwenHandler._request_observation and compares
call IDs to FAKE_FUNCTION_CALL_ID. It sets function_result_adjacent from the
initial-request class rather than the result relationship.

Independent no-network counterexamples: a structurally adjacent function_call/
function_call_output pair with the same fresh ID is called mismatched; an
unpaired output with the fake literal is called matching. Replace these fake
assumptions with bounded correlation of the actual validated returned call and
the admitted continuation within the correct operation/session/lifetime. Do
not infer a call/result relationship from the presence of tools or string
mentions of a type. IDs remain opaque and transient; no raw ID/content artifact.
Reuse the existing observer and accepted Gateway validator, not another validator
or relay. Test multiple non-fixture IDs, mismatches, unpaired/reordered results,
wrong ownership/lifetime, ID-less item with mandatory matching call-ID, and
literal type names appearing only in ordinary text. Compare direct observations
with an independent synthetic oracle, but never use that oracle as acceptance.

005-ab weakened provider.idless_continuation_supported to allow present OR omitted;
its report acknowledges only present was observed. Restore the separate omission
requirement. A present-ID control cannot PASS the ID-less row. If unmodified
Codex supplies IDs, report its natural observed shape honestly. A companion
matrix request derived from an actually returned tool call may exercise omitted
item-ID with the exact Gateway call-ID-HMAC contract, provided it fits/replaces
an already-authorized matrix request and does not increase any request/inference
budget. Keep natural real-Codex tool roundtrip and companion evidence distinct.
No client patch, fabricated omission, call-ID removal or capability assumption.
If no bounded mapping exists, report the exact missing predicate, not PASS.

## C. Bound retained event categories, not ordinary legal SSE length

_StreamState appends every event occurrence to a32-element event-type list.
Independent reproduction:48 valid events containing40 text deltas,9579 total
wire bytes,max frame660, pass the exact Gateway50dcc validator but Local marks
overflow and terminal invalid after the33rd occurrence.

Retain bounded distinct closed event categories plus scalar counts. Do not
reject repeated legal deltas merely because their type has already appeared.
Keep16KiB/event,128KiB/stream and bounded category storage; no full-stream buffer
or larger byte/inference budget. Require a Gateway-validated long-delta stream
well within those byte limits to pass the connected observer, across split and
coalesced chunks. Genuine malformed lifecycle, frame/stream overflow, timeout,
disconnect or cancellation must still fail and close exactly once. Do not
replace the exact Gateway validator with an always-accepting stub for this proof.

## Qualification, evidence and safety

Run focused A–C negative/positive regressions, then full existing real-Codex0149
fake37/37 C1–C5/D and shared protected-mode synthetic29/29 with fake semantic
oracle unavailable. Re-establish every signing/identity/replay/tamper/tool/HMAC/
image-history/compiler/cache/rehydration/isolation/accounting/privacy/cleanup
and fake rollback/cutover-refusal predicate. Run the existing four serial healthy,
post-inference failure, combined finalization failure and genuine zero-access
mapping/dependency failure cases. Preserve exact counts, original primary plus
secondary failures, all selected dispositions, zero pending accounting and exact
cleanup. Fake-only qualification remains protected_acceptance=false.

Run frozen full pytest/Ruff/format/mypy/compileall/shell/build/wheel-boundary/
diff/pin/privacy checks and required CI. Routine local build setup belongs to
coding; report unsuccessful commands and safe alternatives honestly. No human
terminal labor. Do not change project/lockfile versions or protected packages.

Retain sanitized machine evidence under oap/evidence/005-ac before report.
Include exact commands or explicit bindings for all command parameters, tested
SHA, source/import identity, pins, artifact hashes, row IDs/statuses, exact
per-kind/lifetime counts, SSE versus JSON, accounting/cleanup and remaining gaps.
Code edits after tests require requalification; docs/evidence-only reuse needs
exact diff/ancestry attestation. Current docs must not repeat prior overclaims.

Allowed edits: existing rehearsal/tests/helpers/tests/current docs/bounded
evidence/transcript only. No src/, Gateway/PR changes, new subsystem, dependency/
lock/version/binary change or installed cutover. Codex exact0.149.0 binary:
/synology/homes/janezp/.codex/packages/standalone/releases/0.149.0-x86_64-unknown-linux-musl/bin/codex;
SHA256 bbc3341e44c9ead340ed9570c17be936e37870f570751a941699ffd04d672827.
Preserve the OAP executor's actual profile/provider and both active profiles.

Protected host hinton1: vision unit active/running PID23961, start
Sun2026-09-06 18:57:26CEST,NRestarts0,18020listener/health200; text inactive/
MainPID0 expected; seven existing Qwen changes;18021/18030/18031 free. Preserve
qwen3.8-27b/context100000/one sequence/image. Read-only unit/PID/start/restart/
listener and unauthenticated health-status facts only. No protected credential/
environment/config/log/model reads, authenticated/model/compiler/inference/
vision/diagnostic calls, Qwen/checkpoint/patch/venv/unit/launch/GPU/key/network/
firewall/VPN/profile mutation, restart or instrumentation. No second model.
Synthetic guards must fail on accidental real protected access.

Task candidate18031/Gateway18030; existing disposable PostgreSQL16 loopback/
tmpfs/--rm and bounded Docker setup allowed. No host daemon/package/privileged
network change. Private disposable client/cache state; clean only exact owned
targets. No raw secrets, prompts/source/images/tool/SSE/model content, IDs,
signatures/nonces/private endpoints or arbitrary errors in artifacts/logs/cache.

## Immutable publication and return

Amend PR7 only. Push intended changes/evidence plus exact order/active, inspect
CI, capture literal implementationSHA, then atomically publish exactly
oap/reports/005-ac-remove-fixture-assumptions-from-acceptance.md with
Implementation head SHA and Report publication commit: SELF. Final commit only
the report, parent exactly implementationSHA; verify remote head/parent/path/
bytes before response FIFO OK. Never merge/auto-merge/create another PR.
COMPLETE requires all requirements above; otherwise truthful PARTIAL/FAILED.
Separate IMPLEMENTED/TESTED from REAL-E2E ACCEPTED/CUTOVER ACCEPTED/MERGED/
RELEASE-READY. Real protected matrix/cutover remain NOT RUN here. Return evidence
for strategic review without requesting routine human authorization.
