# OAP Work Order — 005-am

## Objective and authority

Resolve ONE concrete ownership question: the forced zero-argument local_lookup
companion versus the exact Gateway Codex0.149 function-call lifecycle requiring
a prior non-empty argument delta. Correct the unsupported runtime-privacy
projection without protected traffic. This is not another generic proof round.
AMEND_EXISTING_PR; Objective005; NO NEW PR, no merge or release.

- Repository ulfe-lmi/slaif-local-coding; PR7:
  https://github.com/ulfe-lmi/slaif-local-coding/pull/7
- Base/head main / oap/005-gateway-ingress-integration.
- Required immutable start005166bfb9bf0c721aadd03c570873d766599a9d;
  report-only parent431c21ce1753c4864765025aa8ca86575975c65b;
  tested005-al source88b94e6669698a551e1ac9affac8716bd814e18d.
- Main570bd2b24ad4b041a07e0320d5ed44bc73e99ad5; PR OPEN, final test SUCCESS
  run34535337987/job103065515758. Head/worktree and005-am availability reconciled.
  Preserve unrelated empty untracked Local, clean, unchanged; no deletion/commit.
- Gateway executable50dcc3b85d614eb1d0c6196595bf22ef5779f846;
  merged authorityd142fd7f04c46fac3469b9b9bba1bd2068aabad8;
  accepted reportad1c556a539130c08dc1063b3e9a25a0af2e5a85;
  app treec0204deaff3cfd055a25f29a7f5d8d3c5e161d57. Exact clean fixture verified
  at /tmp/slaif-gateway-005-aj-PVesAw. Do not modify Gateway or its PR state.

Read governance, this order and005-al report/evidence. Preserve005-al as immutable
FAILED history; do not rewrite or rerun it. Human accepts its ordinary real
Codex/tool and full-image/resumed-crop checkpoints as operational viability and
authorizes controlled development use of that EXACT tested stack. This is not
full Objective005 acceptance, merge, deployment, service/profile change, public
binding or release authority. No such operational change is requested here.

## A. Source-level ownership first; primary hypothesis, not conclusion

Strategic inspection verified Gateway providers/streaming.py:
_validate_codex_function_event rejects an empty delta; arguments.done also
requires membership in _function_delta_seen and equality to accumulated delta
text. output_item.done separately requires _function_arguments_done membership.
Inspect the exact predicates and corresponding Local companion request.

Installed vLLM0.27.1 source, read-only:
/synology/homes/janezp/qwen-serving/venv/lib/python3.12/site-packages/vllm/entrypoints/openai/responses/
- streaming_events.py SHA256
  cf1d8f5e0619148374ce10be15b1a9f7640016d810f1fe766c2dd451a918aa1f
- serving.py SHA256
  628429902ff26b87f86eae1a45297f647f3712d7b421ca9a4866a3fd0f046a5b
Both observed source mtimes precede the unchanged service start. The running
MainPID's executable uses this venv. No model or credential was read by strategy.

The simple emitter opens a tool item with empty accumulated text and no emitted
argument delta. SimpleStreamingEventProcessor.emit_delta emits only when combined
argument text is truthy. emit_simple_tool_call_done emits arguments.done only if
a delta was emitted, but emits output_item.done regardless. The serving loop
closes the current item at end of the stream. This identifies a concrete possible
lifecycle mismatch, not proof that005-al observed that exact variant. Trace the
zero-argument/named-tool parser and normal versus incomplete finish paths to
decide whether the companion is malformed, a legitimate dialect is rejected,
or existing evidence remains insufficient.

Official documentation confirms functions with no parameters are valid, but the
general streaming guide does not settle this zero-delta edge case:
https://developers.openai.com/api/docs/guides/function-calling
Do not claim that documentation alone proves a mandatory/non-mandatory delta.
Use exact installed/pinned source and small non-protected structural fixtures.
Do not import vLLM in a way that initializes GPU/model state; source inspection
and CPU-only synthetic validation suffice for source analysis.

If source/immutable evidence proves a malformed or unrealistic companion, correct
that demonstrated harness-only defect with non-protected tests. A zero-argument
function is not itself invalid. Do not add dummy arguments, fabricate deltas,
weaken the validator/HMAC contract, strip the test or rewrite history merely to
avoid a legitimate provider dialect. If source establishes that a legitimate
provider lifecycle is rejected, prepare the precise Gateway handoff and skip
protected traffic/full matrices; no Local compensation patch.

## B. At most ONE narrow diagnostic, only if source cannot decide

If A cannot distinguish the cases, human authorizes at most one protected
diagnostic request devoted solely to this zero-argument initial companion.
Do not call the full 29-row protected matrix or execute later acceptance steps.
The unused005-al Attempt2 authority does not carry over.

Use the exact existing zero-argument forced request and pinned Gateway validator.
If a direct provider diagnostic is used, mechanically verify its upstream request
shape against the existing Gateway/Local normalization path using non-protected
fixtures; label it diagnostic-only, never full-chain acceptance. Reuse existing
bounded transport/setup helpers; no new observer, generic diagnostic subsystem,
matrix variant or provenance framework. Verify the narrow path against a fake
transport before protected use: at most ONE Qwen inference dispatch, zero compiler
or other protected health/models/probe requests. Read-only process/listener facts
replace extra protected readiness traffic. No retry or second diagnostic request.

Retain ONLY finite failed event/type class, exact finite Gateway predicate/substage
class, necessary presence/status/sequence booleans or bounded counts, and argument
byte length/class. Derive predicate facts from the exact validator/source state;
do not substitute a permissive validator. Keep the first failure frozen. No raw
event bodies, argument/response text, prompts, images, source content, IDs/digests,
credentials/signatures or arbitrary exception text in artifacts/logs. Transient
parsing needed for normal validation must be discarded, not captured as evidence.
Preserve the unchanged32-token request ceiling and131072-byte response/frame cap.

After ownership:
- Legitimate provider dialect rejected by Gateway: hand off the smallest exact
  compatibility question/defect, with finite lifecycle/predicate facts, source
  pins and remaining uncertainty. No Gateway patch, Local compensation or matrix.
- Actually malformed/incomplete provider behavior: report that precisely; do not
  label normal truncation a product defect without evidence. Stop.
- Demonstrated harness-only defect: fix it in this same round, preserve the
  intended ID-less canonical call-ID/HMAC ownership contract, focused-test and
  exact-source fake37-qualify, then run at most ONE final existing protected matrix
  under the unchanged safety/budget envelope. No additional diagnostic/matrix.
- Still unresolved after the one diagnostic: publish the bounded uncertainty and
  stop; never widen capture or make another request to obtain a nicer report.

Do not start any full protected matrix before a harness-only ownership result.
The present human decision prohibits merging PR7 even if that conditional matrix
passes. Return to strategy; no deployment/public binding/service change/release.

## C. Correct runtime privacy projection with NO protected traffic

005-al's artifact/report assert runner_logs_secret_free=true, but its actual
early-stop result has no logs_secret_free field and C5.2 is NOT RUN. This is not
evidence of a disclosure. Preserve the immutable report/artifact and publish an
additive correction, not a rewrite.

Use the smallest existing-runner/evidence projection helper: true only from actual
retained run evidence, false retained as failure, missing/invalid/unexecuted data
as NOT RUN/UNKNOWN. Never hard-code/default to true or infer runtime-log privacy
from empty stderr, source scans or artifact scans. Focused non-protected tests
must cover true/false/missing/invalid and the existing early-stop case. Use that
projection for this round's artifacts. No new evidence framework or protected
matrix solely for this correction.

## D. Exact safety and immutable return

Existing user vision service active/running on hinton1: PID23961, UID1029,
startSun2026-09-06 18:57:26CEST, zero restarts;18020 listening, seven pre-existing
Qwen changed paths; text unit inactive. Preserve qwen3.8-27b vision/model/GPU/
context100000/one-sequence/image configuration. Candidate127.0.0.1:18031 and
verified-free ephemeral loopback Gateway port only if the narrow workflow needs
them; no installed service or public binding. Existing coding wrapper/profile
oap-coding-luna-xhigh was independently confirmed idle on control.fifo.

Exact Codex0.149 binary remains
/synology/homes/janezp/.codex/packages/standalone/releases/0.149.0-x86_64-unknown-linux-musl/bin/codex
SHA256 bbc3341e44c9ead340ed9570c17be936e37870f570751a941699ffd04d672827.
No Local src/, Gateway, dependency/lock/binary, Qwen/model/checkpoint/patch/venv/
unit/launch/config/GPU/network/firewall/VPN/credential/profile changes. No model
loading/import side effects, restart, second GPU process or credential discovery.
Only read the exact active vision MainPID's unique nonempty VLLM_API_KEY, in bounded
memory as UID1029 through the existing approved procedure if the diagnostic or
conditional matrix is actually needed. Never print/hash/persist/argv/public-metadata
the credential. Private dirs0700/files0600 and existing owner/no-follow/single-link
guards unchanged. No whole-run sudo; existing bounded PostgreSQL16 loopback/tmpfs
helper only if needed. Coding owns safe setup and exact owned cleanup.

For any conditional final matrix: existing900-second/64-observation/one-active-
dispatch/nine-macro-operation/compiler/inference/output bounds unchanged, zero
internal retries, all existing C1-C5 and fake D predicates required. A first
unexpected failure stops later dispatch. No partial checkpoint implies full
accounting/privacy/semantic acceptance. No protected traffic for source-only or
privacy-only work. State exact zero/one diagnostic and zero/one matrix counts.

Allowed edits: bounded existing harness/projection helpers, focused tests,
directly relevant ownership/handoff documentation, additive privacy correction
and005-am evidence/transcript. No generic framework or unrelated qualification.
Run focused tests and current required CI/package/static gates. Full fake37 is
required only for the harness-fix/final-matrix branch, not for a Gateway handoff.

Publish one immutable report plus the precise Gateway handoff if indicated,
distinguishing source proof, actual diagnostic observations, historic attribution
uncertainty, operational development permission, full acceptance and release.
Keep the handoff in this PR/strategic-facing artifacts; coding does not alter
Gateway code, PR state, orders or FIFOs. Strategy owns cross-repository handoff.

Amend PR7. Push non-report work, capture literal implementationSHA, then publish
exactly oap/reports/005-am-zero-argument-ownership-and-privacy.md with
Implementation head SHA and Report publication commit: SELF. Final commit only
that report, first parent implementationSHA; verify remote head/parent/path/bytes
and checks, then exact response FIFO OK. No later mutation, coding merge or
automatic next suffix. PR7 remains unmerged/not release-ready under current
human authority; preserve all older reports and unrelated files.
