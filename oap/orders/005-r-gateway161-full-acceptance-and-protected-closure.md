# OAP Work Order — 005-r

## Objective and authority

Resume Local Objective 005 on existing PR #7 using the accepted, merged Gateway
Objective-161 compatibility correction. Run the complete existing Local fake
matrix, including actual Codex 0.149 same-session full-image → resumed crop
history. Re-establish all selected C1–C5/D predicates. Only after the entire
fake matrix passes may the existing authorized bounded protected matrix run.
Publish one immutable report and return to strategic review. No coding merge.

The human explicitly delegated this continuation on 2026-09-09 and directed
acceptance and closure. Gateway's direct synthetic acceptance does not prove
the new real-Codex vision/resume or protected matrix; those are this round's
responsibility. 005-q remains immutable pre-fix evidence. Do not execute the
old 005-q round, rewrite its report, reinterpret its result, or change earlier
orders/reports. This is fresh 005-r evidence against a corrected dependency.

## Independently reconciled GitHub state

- Repository: `ulfe-lmi/slaif-local-coding`; objective `005`, round `005-r`.
- Mode: `AMEND_EXISTING_PR`; **NO NEW PR**.
- Existing PR: https://github.com/ulfe-lmi/slaif-local-coding/pull/7
- Base/head: `main` / `oap/005-gateway-ingress-integration`.
- Remote main: `570bd2b24ad4b041a07e0320d5ed44bc73e99ad5`.
- Required starting PR/checkout head:
  `5aec2beccc07432d45e936b82952abf52dfb10d8`.
- Its literal implementation parent:
  `64e50172ee02563e2b021554f6b0d345cc7dfdec`.
- It contains only `oap/reports/005-q-close-fake-orchestrator-obligations.md`.
- PR OPEN, non-draft, MERGEABLE/CLEAN, sole open Local PR; current `test`
  SUCCESS. Local checkout clean before activation; 005-r order/report absent.

Gateway handback (read completely and reconcile live):
https://github.com/ulfe-lmi/slaif-local-coding/pull/7#issuecomment-5601188832

- Exact executable, non-report pin:
  `50dcc3b85d614eb1d0c6196595bf22ef5779f846`.
- Merged Gateway main authority:
  `d142fd7f04c46fac3469b9b9bba1bd2068aabad8`.
- Production behavior introduced at:
  `899bd57e6eef49149d54c65838cf25d043e34a55`.
- Final immutable report:
  `ad1c556a539130c08dc1063b3e9a25a0af2e5a85`, path
  `oap/reports/161-r-bound-snapshot-and-retain-returned-history.md`.
- Report is PASSED, report-only, parent exactly `50dcc3b...`.
- Accepted app tree: `c0204deaff3cfd055a25f29a7f5d8d3c5e161d57`.
- Merge/report complete tree: `5d03e894d9d3c8839c807ef7cd1ad931d4e3aab7`.
- Gateway PR #298 merged; merge parents are prior main `910ddaa...` and final
  report `ad1c556...`; executable pin is reachable from this merge.
- All ten report-head checks SUCCESS: Unit/lint/migration, PostgreSQL,
  OpenAI-compatible E2E, Playwright, Docker Compose, Documentation hygiene,
  Analyze Python, Analyze (python), Analyze (javascript-typescript), CodeQL.

Use a clean detached task checkout of `50dcc3b...`, not a moving branch or
report commit. Keep implementation, production, report, merge, and app-tree
facts separate. No Objective-155 historical machinery or Gateway mutation.

## Host, executor, and protected-resource scope

Strategic read-only preflight found host `hinton1`, vision unit
`qwen-serving-vision.service` active/running, MainPID `23961`, start
`Sun 2026-09-06 18:57:26 CEST`, zero restarts; model `qwen3.8-27b`, context
100000, max sequences 1, image limit 1, no language-model-only flag, existing
port 18020 listener, health HTTP 200. Text `qwen-serving.service` is inactive.
Ports 18021/18030/18031 are free. Seven pre-existing Qwen checkout changes
remain unrelated protected state.

Existing FIFO objects are owner-only FIFOs. The coding wrapper has a waiting
control reader. Runtime names `oap-coding-luna-xhigh`; this profile was absent
from current user config in strategic preflight. Do not silently change host
profiles/model/provider to resolve that discrepancy. The acceptance client is
separate: exact task-controlled Codex 0.149.0 with SHA-256
`bbc3341e44c9ead340ed9570c17be936e37870f570751a941699ffd04d672827`.

Reconcile baseline again before any live traffic. Preserve Qwen port 18020,
checkout, model/checkpoint/patches/venv, units/launch/GPU/context/image flags,
credentials, firewall/VPN/network bindings, and active OAP profiles. No restart,
switch, edit, extra model, broad credential discovery, or service repair.
Ordinary candidate Local binds 127.0.0.1:18031; the existing disposable Gateway
uses a task-owned loopback port. Preserve existing bounds and topology.

After full fake PASS only, the existing 005-o credential scope is available:
resolve MainPID from the exact vision unit, require unchanged active identity,
read only the unique nonempty `VLLM_API_KEY` entry from that PID's environment
into memory, pass it only to the harness as `QWEN3090_API_KEY` or in-memory
upstream header, then discard it. Never print/hash/store it or expose it in
argv, reports, other process output, Codex input/config, or Gateway metadata.
Never source the Qwen environment file or read unrelated environment entries.

## A. Preflight and bounded Local changes

1. Verify exact Local PR/HEAD/report parent and immutable transcript. After
   activation only this order and `oap/active=005-r` are expected differences.
2. Verify Gateway handback, executable pin, report parent/path/PASSED state,
   merge ancestry/tree and successful required checks. Attest actual imports
   from the detached checkout; do not mutate it or copy Gateway logic.
3. Update the existing repository-only Gateway pins from `9d247e7f...` to
   `50dcc3b...`; record merge `d142fd7...` separately. Update pin-dependent
   tests/docs without changing historical fixtures or transcript.
4. Use the existing `scripts/gateway_accounting_rehearsal.py` and its helpers,
   manifest, strict fake provider, and exact Codex setup. Fix only necessary
   Local harness integration defects directly evidenced during this round.
   No speculative redesign, protocol discovery, new diagnostic subsystem, or
   Local product-policy change.
5. The historical `protected_mode_not_authorized_005p` guard is no longer a
   policy prohibition for this round after full fake PASS. If enabling the
   existing protected path requires support edits, they are authorized only
   with an explicit fail-closed fake-pass gate, same candidate/pin, unchanged
   budgets, and focused no-model tests before any protected call. Do not merely
   remove a guard and run an unverified or fake-dependent protected path.
6. Keep all current request/byte/event/output/time/concurrency budgets. Record
   numeric per-phase maxima before execution; do not increase them. Reject
   incomplete enforcement before protected traffic. Zero automatic retries;
   first protected failure stops later inference. No alternate prompts,
   model/config changes, manual stream substitution, or bypass qualification.

## B. Fresh full fake acceptance

Run actual Codex 0.149 -> Gateway `50dcc3b...` -> actual Local candidate ->
strict fake Qwen with task-owned PostgreSQL and private Codex home/workspace.
Run every selected existing C1–C5/D obligation. Previously passing 005-q rows
are historical evidence only; re-observe them now.

Require exact two-turn ordinary function lifecycle and ID-less item/mandatory
call-ID continuation with Gateway HMAC ownership; signed identity per admission;
root/dependency acquisition, compiler/cache/injection and same-session zero-root
rehydration; independent session/key/owner/repository isolation; exact and
concurrent replay/idempotency; body/query/path/route/signature/timestamp/nonce
tamper; invalid-key, hosted/dropped-tool, and quota rejection; controlled
synthetic provider failure; per-request reservation/ledger finalization or
release, usage/counter consistency, zero pending/duplicates; compiler calls
excluded from Gateway public accounting; privacy, no bypass, and exact cleanup.

The actual same-session full-image then resumed crop/history interaction must
cross Gateway into Local and reach fake Qwen. Use actual prior assistant output
and actual Codex resume history. Do not substitute a hand-written request,
strip assistant history, convert output_text in Local, change clients, or relax
the manifest. Prove history image multiplicity at Local, newest image only
upstream, governance, and both terminal lifecycles independently.

D predicates remain the existing fake installation/refusal/backup/rollback
tests. They must all PASS, including the previously dependent D6–D9 rows.
Require the exact selected-ID set, runtime producers/projections, all observed
PASS results, `missing=[]`, null first failure, zero retries, and overall PASS.
Record every failed/skipped/not-run/dependency result if it cannot pass.
Neither a successful process exit nor `missing=[]` alone establishes success.

## C. Conditional protected acceptance

Only if B entirely passes on the exact current Local/Gateway candidate, all
required Local checks pass, protected state is unchanged, and the existing
runner can enforce the original budgets/observations, run one bounded matrix:

```text
real Codex 0.149.0 -> exact Gateway 50dcc3b...
 -> actual Local on 127.0.0.1:18031 -> existing protected vision Qwen
```

Re-establish readiness/models/non-streaming, real two-turn function/SSE and
call-ID ownership, governance/acquisition/rehydration, full-image/resumed crop,
isolation, signing/replay/tamper/authorization, accounting/failure, privacy,
no-bypass, and cleanup criteria of 005-o B1–B5 under their unchanged limits.
Use direct independent boundary observations; Gateway ledger rows are not
provider-call counts, status 2xx is not terminal lifecycle proof, and fake
provider observations cannot become protected observations.

Controlled failure remains a synthetic provider path; never deliberately fail
Qwen. Compiler calls remain separately bounded and excluded from public rows.
Stop at the first protected boundary failure, retain only fixed safe code/stage/
counts/relations, clean up, and publish. No protected retry or automatic Gateway
patch. If protected execution is not possible within the existing authorization
or harness guarantees, report the precise NOT RUN reason; do not mark complete.

This round requires D refusal/rollback predicates and protected cleanup. It
does not automatically turn fake D evidence into actual installed cutover.
Any already-authorized ephemeral installation under 005-o C remains strictly
conditional on complete protected acceptance, exact restorable backups, current
refusal gates and unchanged service/profile scope. If not performed, explicitly
leave CUTOVER ACCEPTED=no and preserve it as an open Objective-005 criterion.
No persistent deployment, public bind, active-profile change, or release.

## D. Verification, documentation, ownership, publication

Run frozen pytest (full and affected), Ruff check/format, mypy src/tests,
compileall, shell syntax, build wheel/sdist, package boundary, diff, pin,
secret/raw-output and historical-hook checks. Run every necessary affected
check after an edit; do not infer acceptance from unrelated green CI.
Update current testing/integration/runbook/criterion documentation to reflect
the new pin, actual fake/protected outcomes, and remaining limits. Preserve
005-q and all earlier terminal artifacts byte-for-byte. No readiness inflation.

Report direct failure ownership from actual boundary evidence. A Gateway error
event alone does not establish a Gateway product defect. Return to Gateway
only if the corrected merged Gateway demonstrably owns a new defect. Do not
modify Gateway, reopen PR #291, change Qwen, or compensate through Local policy.

Use synthetic fixtures only. No raw prompts/source/images/model text/SSE/tools/
credentials/identity/signatures/nonces/private URLs/arbitrary errors in logs,
reports, metrics or evidence. Use fixed codes, booleans, bounded counts/timings,
safe versions and synthetic hashes. Preserve actual request semantics in transit.
Use existing task-owned loopback PostgreSQL 16/tmpfs/--rm, finite readiness,
no privileged/host networking or persistent DB. Sudo only for existing bounded
Docker operations. Preserve .venv and unrelated state. Exact cleanup only;
no broad deletes or host-Codex-history/cache access.

Coding owns routine setup, bounded support edits, testing, commits and evidence.
Do not recruit human/strategy for routine terminal work. No new PR or merge.

Push all implementation and exact order/active bytes to existing PR #7, verify
CI, record literal 40-hex implementation SHA, then atomically create exactly:
`oap/reports/005-r-gateway161-full-acceptance-and-protected-closure.md`.
Include `Implementation head SHA: <literal>` and
`Report publication commit: SELF`. Final commit changes only that report and
its first parent equals the literal implementation SHA. Push and independently
verify remote PR head/parent/path/report bytes, then send exact response FIFO OK.

Report exact Gateway implementation/production/report/merge/app-tree and Local
start/implementation/report facts; all selected fake C1–C5/D rows/projections;
protected rows or exact NOT RUN reason; per-phase budgets/attempt counts;
accounting and cleanup; protected before/after state; limitations and checks.
Distinguish IMPLEMENTED, TESTED, REAL-E2E ACCEPTED, CUTOVER ACCEPTED, MERGED,
RELEASE-READY. The last two remain no; each earlier label needs scoped proof.
Return to strategy for independent acceptance and merge review.
