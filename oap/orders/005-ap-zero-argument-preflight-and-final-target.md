# OAP Work Order — 005-ap

## Objective and reconciled authority

Objective 005: restore the intended zero-argument target, enforce its exact
Gateway-derived semantic preflight, and perform ONE final targeted identity pair.
AMEND_EXISTING_PR, NO NEW PR. This is not a diagnostic round or full protected
matrix. No automatic suffix after005-ap. Coding never merges.

- Repository: ulfe-lmi/slaif-local-coding; PR7:
  https://github.com/ulfe-lmi/slaif-local-coding/pull/7
- Base/head: main / oap/005-gateway-ingress-integration.
- Required starting HEAD:9ef4c53e2067cd4ef723b2a42a0a645fc3f924aa,
  immutable BLOCKED005-ao report; parent ea0aad3dd52acb2b951d330b30e0bff4d6e459cd.
- Main:570bd2b24ad4b041a07e0320d5ed44bc73e99ad5. PR OPEN/CLEAN; exact final
  test SUCCESS34694332074/job103555088682.005-ap unused after reconciliation.
- Preserve005-ao and every older order/report. Its failed target is NOT product
  evidence: introduced required path conflicted with the no-argument prompt and
  removed exact Gateway zero-argument eligibility. Do not rewrite/rerun005-ao.
- Preserve unrelated empty untracked Local, clean, unchanged. No deletion/staging.

Gateway accepted executable/merged-main authority:
5ea38325ef3a3ebc69524b4679b795fab0c52935;
production732e3bad17d93909f210321b97bedd8e5718fb7b;
app treea7b64d35650b61fbba3558ddb519c6e52a627ec9;
full tree1ede9cea41c566b141441ad9a3122133d5fc1ff6;
codex-0.149-responses-v1 module version4. GatewayPR299 merged; strategy
independently verified current main/tree and all ten final-head checks SUCCESS.
No Gateway modification, branch authority substitution, or historical155 machinery.

Reuse exact clean /tmp/slaif-gateway-005-ao and task venv
/tmp/slaif-gateway-venv-005-an.THUKHt after verification; SDKopenai2.41.0 and
existing PostgreSQL16 prerequisite. Avoid repeating resolved setup. Create a clean
Local qualification worktree at the committed candidate; preserve primary dirt.

## A. Restore the exact target — ZERO protected traffic

In scripts/gateway_accounting_rehearsal.py, restore _identity_companion_tools()
local_lookup declaration to the intended zero-parameter schema:

    {"type":"object","properties":{},"additionalProperties":false}

No nonempty required set. Keep its initial instruction requesting local_lookup
with no arguments, streaming request, current forced function choice, store=false
and32-token bound. Do NOT introduce required parameters to exercise varied-argument
preservation. Those variations belong in separate model-free/fake unit fixtures.
Ensure the target's fake provider reply is legal for this zero-argument fixture;
do not change the real response, synthesize events or canonicalize returned args.

Preserve the correct005-ao continuation: actual validated completed function_call
followed by matching function_call_output, omitting only optional function-call
item id, preserving actual canonical call_id/name/arguments/status/legal namespace
transiently. Both companion requests use the existing reviewed streaming contract;
do not restore the artificial non-stream history request. Same session/owner/
repository/key/scope/HMAC relationship, no fixture-name/argument substitution.
Preserve orphan rejection, including when a preceding HTTP request seeded state:
the required preceding call must still exist in this continuation's ordered input.

## B. HARD semantic gate before credential resolution or any protected request

Construct the ACTUAL initial request once, before _select_protected_runtime or
any other protected credential acquisition. Use exact pinned Gateway helper:

    codex_0149_zero_argument_function_names(initial_request)
        == frozenset({"local_lookup"})

Also construct the exact validator using _gateway_stream_validator_factory and
require its profile.zero_argument_function_names equals that same exact set.
These are runtime hard checks, not advisory report booleans or generic fake PASS.
Any mismatch, missing profile/helper or exception stops before credentials and
before provider health/models/readiness/inference traffic. Keep checks effective
in normal execution (do not rely on an optimization-removable Python assert alone).

Bind the checked request to the first request actually transmitted: hold the
constructed body/encoded bytes privately and reuse it, without later rebuilding
an unchecked request. A pre-dispatch comparison may supplement the immutable
request binding but cannot replace the BEFORE-CREDENTIALS eligibility check.
Do not print/persist body, generated session IDs or their hashes. Retain only
finite equality/result booleans and bounded counts needed to prove this gate.

Tests must prove failure ordering with credential/provider spies: changing target
to required path, changing profile fact, or otherwise failing the exact predicate
must produce ZERO credential resolution and ZERO protected requests even if the
generic fake prerequisite is presented as passed. The sent initial request must
be the semantically gated one. No alternate target path may bypass this check.

## C. Focused/model-free/fake qualification

Use existing helpers and bounded tests, no new framework/refactor. Prove:

- Exact constructed target is eligible; adding required path is ineligible.
- Exact Gateway v4 profile contains the expected fact.
- Legal zero-argument omission lifecycle and ordinary canonical argument lifecycle
  both pass the exact Gateway validator; do not demand that real Qwen choose one.
- Actual returned fields survive legal call+output replay; optional call-item id
  alone omitted. Test arbitrary/nonempty argument preservation separately from
  the protected zero-argument declaration.
- Orphan, wrong order, mismatched/missing call-ID and wrong session/key/owner
  remain rejected through existing focused/fake tests.
- Target selector still excludes ordinary Codex, vision, other identity operations
  and full protected matrix; max2inference,0compiler. Keep005-ao ledger/count,
  host-preservation and actual before-deletion privacy-scan gates intact.

Run normal frozen unit/static/type/build/package checks, exact Gateway-dependent
focused tests, the existing real-Codex fake37/37 gate and the isolated fake target
qualification at the final execution source. Generic fake success is NOT the
semantic gate.8 opt-in protected pytest tests are not extra traffic authorization.
Finish code/typing/current-facing docs before freezing the candidate to avoid
unnecessary fake reruns for documentation alone. Later report/evidence-only
commits do not require rerunning already-qualified execution blobs; keep the
qualified clean candidate and cite its exact tested SHA separately from SELF parent.

Current-facing doc corrections are within this same closure, not another round:
docs/ADAPTER-CONFIGURATION.md still says Gateway does not emit signed identity;
config/adapter.example.toml has the same stale COMMENT. Correct those dated claims
without changing example configuration values or any installed configuration.
No packaging redesign/release or deletion of unrelated files; build from the
clean candidate so unrelated primary files are not packaged accidentally.

## D. ONE targeted protected pair; same-response safe failure evidence

Only after A-C pass may the existing target run against exact Gateway5ea38325,
the unchanged Local production candidate and unchanged protected vision Qwen.
Exactly one target attempt, max TWO Qwen inference requests, ZERO compiler calls.
First request is the exact zero-argument request fromB; continuation only after
the first successfully yields its validated call. No ordinary Codex/vision/full
matrix rerun. No retries once any protected request begins. Preserve existing
bounded necessary preflight only and900s/64observations/one-active-dispatch/
131072-byte PER RESPONSE+frame/32-output-token ceilings. No budget increases.

Require existing paired identity/HMAC ownership, signing, both legal terminal
responses/usage, actual reservation AND ledger terminal/count consistency, zero
pending/duplicates, privacy and exact owned cleanup. Accept whichever legal
zero-argument serialization exact Gateway162 accepts; no provider-event rewriting.

If either request fails, send NO further request. From that SAME response retain
HTTP status and allowlisted provider error type/code/param or finite failed
validator/event-stage classification where available. In particular, preserve
the finite failing SSE event/type stage when the current parser has it; do not
discard available frame-class information and retain only gateway_validator.
Use the existing bounded parser/observer paths, not a new diagnostic subsystem.
Unknown/free-form error values map to other/unavailable; never retain raw error
message/body, event contents, arguments, prompts, output, IDs, images, credentials,
signatures or arbitrary exceptions. No extra diagnostic request or broad capture.
Focused fake failures must show error-context projection is bounded/content-free.

Stop and publish on failure, no runtime repair-and-retry afterward. For counts,
use actual dispatch budget and unique direct records;005-ao's aggregate observer
snapshots were duplicated under identity/codex lifetime labels. Do not turn that
known limitation into a general accounting/provenance redesign or claim extra calls.

## E. Protected host / credentials / local authority

Read-only facts: user qwen-serving-vision.service active/running, PID23961/UID1029,
startSun2026-09-06 18:57:26CEST,NRestarts0;18020 same process,18021/18031 absent,
seven pre-existing Qwen changed paths. Preserve Qwen3.8-27B vision/vLLM0.27.1/
RTX3090/context100000/maxseq1/one-image/model/config/process/GPU/credential state.
No Local src/, Gateway, dependencies/versions/lock, Qwen checkout/model/patch/venv/
unit/launch/service/network/firewall/VPN/credential/profile mutation or public bind.

Exact Codex0.149 binary:
/synology/homes/janezp/.codex/packages/standalone/releases/0.149.0-x86_64-unknown-linux-musl/bin/codex
SHA256bbc3341e44c9ead340ed9570c17be936e37870f570751a941699ffd04d672827.
Executor stays hosted OpenAI, oap-coding-luna-xhigh, gpt-5.6-luna/xhigh. Strategy
removed ONLY its duplicate idle wrapper; terminal wrapper1816490/reader1816494
is the sole verified control recipient. No model/profile selection change.

After the hard semantic gate only, use existing in-memory procedure: exact active
vision MainPID's unique nonempty VLLM_API_KEY, UID1029; never print/hash/persist/
argv/metadata-expose it or broaden discovery. Discard transient credential/call
references. Existing0700dirs/0600files/owner/no-follow/regular/single-link guards,
no whole-run sudo. Scoped PostgreSQL16 loopback/tmpfs, Local127.0.0.1:18031,
verified-free ephemeral loopback Gateway and exact owned cleanup only. Coding
owns routine safe setup. No persistent deployment/cutover/release.

## F. Correct composed closure / immutable report / strategic decision

On target PASS, compose it with human-accepted REAL LOCAL005-an evidence:

- Local report713492fa146372be13073c10a98d8f045e71900a.
- Local implementation/evidence parent556ca152a589747b5c9adeca3d44de56e61ce08c.
- Tested Local source8245f1c0270e0a314058f2afa77c9f6e80ace483.

These are not Gateway report SHAs.005-ao's erroneous attribution is corrected by
https://github.com/ulfe-lmi/slaif-local-coding/pull/7#issuecomment-5645970458
Reuse accepted ordinary-Codex/tool and full-image/resumed-crop checkpoints with
their precise scope; do not rewrite old reports or claim a newly executed29-row
protected PASS. No further protected matrix is required by current human authority.

Publish exactly oap/reports/005-ap-zero-argument-preflight-and-final-target.md,
with literal tested Local/Gateway pins, semantic gate/body-binding/ordering proof,
focused/fake/target results, actual request counts, safe failure facts if any,
accounting/privacy/cleanup/host evidence and correct Local checkpoint references.
Distinguish IMPLEMENTED, TESTED, REAL-E2E ACCEPTED BY COMPOSITION, CUTOVER ACCEPTED,
MERGED and RELEASE-READY; coding never claims its own merge. No installed cutover
or release. Update PR description to truthful cumulative scope/current outcome.

Push all non-report work and unchanged active/order; capture literal Implementation
head SHA; publish one report-only SELF child whose parent is exactly that SHA.
Verify remotePRhead/report/parent/path/bytes/checks, send exact response FIFO OK,
then stop. Strategy independently reviews report/production/package/security and
all exact final-head CI. Under existing human authority strategy alone mergesPR7
if this composed closure and all merge criteria pass, then verifies remote main.
If target fails, publish and STOP with no merge or automatic005-aq. Controlled
development permission remains separate. No release/deployment/service mutation.
