# OAP Work Order — 004-aa

## Objective

Amend objective-004 PR #6 in one convergence round to accept the canonical
simulated-compaction state boundary on direct evidence, prove the distinctive
binding rule remains effective after that boundary against the current text
fixture, reconcile every non-vision Objective-004 criterion, and leave one
repository-owned bounded real-Codex vision test fully executable-ready. Do not
search for or require a Codex-native compaction trigger. Do not run live vision
until strategy confirms the human-provided fixture is active.

This identifier is the human-authorized continuation after `004-z`. The human
explicitly directed that genuine immutable continuations use `004-aa`,
`004-ab`, and so on without renumbering history. It remains numeric objective
004, the same branch, and the same PR.

## GitHub objective state

- Repository: `ulfe-lmi/slaif-local-coding`.
- Numeric objective / round: `004` / `004-aa`.
- PR mode: `AMEND_EXISTING_PR`; **NO NEW PR**.
- Existing PR: #6, `https://github.com/ulfe-lmi/slaif-local-coding/pull/6`.
- Required base: `main` at
  `7a2c36a0a40958a6059a765c2f9d5e5bf4ddc161`.
- Required head: `oap/004-real-codex-governed-e2e`.
- Current verified remote head / `004-z` SELF:
  `5d2cd6bce1702f04c603bc434e473a90fd00e7d0`.
- PR is OPEN, non-draft, MERGEABLE/CLEAN; current required `test` check is
  SUCCESS.
- Never merge or enable auto-merge; strategic alone reviews and merges.

## Authoritative scope correction

Canonical `ARCHITECTURE.md` MVP acceptance item 7 requires a distinctive
binding rule to remain available after **simulated/actual compaction**. Section
8.4 defines the product property as stable constitutional injection independent
of what client history retains. Existing objective-003-e code deliberately
models the boundary as a later zero-root/new-context request under the same
complete static identity.

Therefore:

- faithful deterministic simulated compaction is acceptance evidence;
- actual/native Codex compaction is not required;
- lack of a native force-compaction command is only upstream observability;
- `004-u` through `004-z` remain immutable historical evidence but their
  native-trigger gate is superseded by this human decision;
- do no more context exhaustion, native-compaction searches, Codex session
  reverse engineering, workspace-write, bubblewrap, sandbox, or environment
  diagnostics.

## Strategic prepublication compatibility edits

Strategy made exactly two necessary OAP-validator edits before atomic
activation because the human-authorized `004-aa` suffix was rejected by the
old single-letter regex:

- `oap/bin/publish_order.py`: accept one or two lowercase suffix letters;
- `oap/bin/check_state.py`: accept the same one- or two-letter form.

Preserve and commit these exact control-plane changes. Test that legacy IDs and
`004-aa` pass while empty, uppercase, numeric, three-letter, and malformed IDs
fail. Do not broaden product behavior or rewrite earlier protocol history.

## A. Hostile audit of simulated compaction fidelity

Re-read full canonical `ARCHITECTURE.md`, then inspect the implementation and
all relevant tests/reports from objective 003-e onward. Establish explicitly:

1. the simulated follow-up request contains no root, delegated source, prior
   conversation governance, or sentinel value;
2. no previous request payload, model response, tool output, or hidden test
   global supplies the follow-up governance;
3. the only retained state is the designed bounded process-local entry of
   validated compiled root/dependency indexes and inclusion metadata;
4. exact principal/route/session/repository/model/source/version/policy/bound
   identity prevents cross-context reuse;
5. the follow-up reruns deterministic selection and API-specific injection,
   without compiler/model compilation work;
6. the injected constitution contains the distinctive delegated binding rule;
7. expiry, corruption, restart, changed identity, changed source, overflow,
   and marker conflict fail safely; changed governance invalidates/replaces the
   prior working set;
8. no raw prompt/source/image/tool-output/request-body/credential is retained
   in rehydration state or emitted in logs/metrics.

If these properties hold, classify the architecture's simulated-compaction
mechanism PASS. Do not weaken the statement into a native-Codex claim. If a
real Local Coding defect is found, fix only that defect in this round and add
the smallest regression test.

## B. Bounded live simulated-boundary governance proof

Use the current protected text model read-only through one disposable candidate
adapter on `127.0.0.1:18031`. Run the minimum deterministic sequence:

1. fresh private cache/static opaque identity and one synthetic root plus exact
   paired delegated dependency bytes containing an ephemeral hidden binding
   sentinel;
2. first Responses request establishes root/dependency observation,
   compilation, cache, working-set selection, injection, and a successful main
   model response;
3. second stateless zero-root/new-context Responses request contains neither
   prior history, governance source, dependency bytes, nor sentinel and asks the
   governed model to comply with the binding rule;
4. prove a rehydration hit and injection, exact binding-sentinel compliance,
   and zero additional compiler-model attempts on the second request.

The sentinel must be generated only inside the disposable fixture and withheld
from the second request/prompt/config. Retain only fixed status labels, counts,
lengths, hashes, and booleans. If the first request cannot establish valid
state, stop truthfully. If rehydration or sentinel compliance then fails, report
the first concrete Local Coding defect and fix it only if bounded within this
round. Do not substitute native compaction or spend calls on retry loops.

## C. Reconcile all non-vision Objective-004 acceptance

Create a concise criterion ledger grounded in source, current tests, accepted
reports, and fresh checks. It must cover at least:

- real Codex 0.149.0 with the human-approved global yolo mode through adapter;
- effective root observation, exact delegated dependency acquisition,
  direct non-recursive compilation, bounded injection, and hidden sentinel;
- second independent invocation with persistent cache reuse and no unnecessary
  compiler-model attempt;
- content-hash invalidation and changed-root replacement;
- principal/session isolation, TTL/LRU/byte bounds, corruption/restart safety;
- text Responses, ordinary tools, multi-turn continuation, and SSE;
- security/privacy/error/header/auth/cache/compiler/logging hardening;
- candidate systemd health/readiness/metrics/text/tool/SSE and rollback proof;
- simulated post-compaction rehydration and binding-rule compliance;
- packaging, provenance, protected-host preservation, and gateway integration
  documented as the separate next milestone.

Distinguish product defects from missing external evidence. If all non-vision
items pass, update `oap/COMPLETENESS.md` honestly so the only Objective-004 gap
is live vision; do not claim Objective 004 complete or merge-ready yet.

## D. Prepare the exact live vision acceptance test

Add or finalize repository-only test support under `tests/` (never the wheel)
so no implementation or planning round is needed after the human starts the
fixture. Keep it small and specific to final acceptance.

The prepared test shall use:

- installed Codex CLI `0.149.0`;
- disposable persistent `CODEX_HOME`, disposable Git fixture, private cache,
  and model catalog declaring `input_modalities=["text","image"]`,
  `supports_image_detail_original=false`, 100000 context, and no parallel tool
  calls;
- Codex-under-test itself in the already human-approved global
  `--dangerously-bypass-approvals-and-sandbox` mode;
- initial `codex exec --image <full.png>` and same-session
  `codex exec resume --image <crop.png>`;
- candidate Local Coding adapter on `127.0.0.1:18031` with route
  `qwen38-vision-codex`, model `qwen3.8-27b`, max images 1,
  `retain_newest`, governance enabled, and protected vision upstream
  `http://127.0.0.1:18020/v1`;
- two deterministic, non-sensitive synthetic PNGs: a full scene with
  distinguishable left/right features and a crop of the newest target;
- the same synthetic governance/delegated hidden-sentinel design already
  accepted for the text E2E.

Before any live fixture is available, prove with fake upstream and focused unit
tests that the runner:

1. uses the exact Codex argv/mode/profile and persists then resumes one session;
2. sends one full image on turn 1 and presents full-history-plus-new-crop on
   turn 2 at the adapter boundary;
3. records safe direct evidence from the body actually leaving the adapter that
   turn 1 contains one supported image and turn 2 contains exactly one supported
   image, the newest crop, with all non-image/tool/governance content preserved;
4. identifies fixtures by expected precomputed hash/length or fixed labels
   without retaining/reporting base64/image bytes or raw request bodies;
5. requires both model responses to succeed and requires the second response to
   demonstrate the crop was processed;
6. requires governance observation/injection and the hidden binding sentinel to
   remain effective on both turns;
7. verifies image metrics: turn 1 seen 1/removed 0; turn 2 seen 2/removed 1,
   or equivalent exact deltas from the captured actual outgoing shapes;
8. bounds subprocess time/output/model calls, scans safe logs/metrics, removes
   candidate/fixture/cache/config/session/observation state, and verifies port
   18031 is absent afterward.

An acceptance-only in-process transport recorder may observe and forward the
candidate adapter's actual outbound request if needed. It must record only
count, supported modality/type, expected-fixture match, length/hash, and fixed
status; never raw images, prompts, sources, tool output, bodies, or credentials.
Do not add production diagnostics, public debug headers, or Local Coding
fallback/bypass behavior.

## E. Exact protected vision fixture contract

Prepare the human handoff against the already-discovered, mutually exclusive
fixture, but do not operate it in this round:

- user unit: `qwen-serving-vision.service`;
- service endpoint while active: `http://127.0.0.1:18020/v1`;
- served model: `qwen3.8-27b`;
- required capability: Responses `input_image`, one image per request,
  `detail: auto`, text Responses, and ordinary function tools;
- expected launch policy: vision tower enabled, no `--language-model-only`,
  `--limit-mm-per-prompt {"image":1}`, context 100000, max sequences 1;
- the current `qwen-serving.service` and vision unit are mutually exclusive on
  the one GPU and shared port, so the human must deliberately stop text and
  start vision; coding/strategy must not do so;
- after live acceptance, the human-controlled rollback is stop vision/start
  text; coding verifies but does not perform protected switching unless the
  human explicitly authorizes it.

Confirm the existing unit/launcher/environment/handoff facts read-only, their
current inactive/active states, and required hashes without exposing the API
key. Do not edit, start, stop, enable, reload, or restore any protected service.

## Non-goals and safety

- No native compaction trigger investigation or compaction heuristics.
- No bubblewrap/workspace-write/sandbox/network namespace diagnostics.
- No live vision request before the strategic human gate is satisfied.
- No protected model/service/profile/key/firewall/VPN/network/systemd mutation.
- No port 18020 change; candidate only on 18031; no duplicate GPU model.
- No gateway integration/cutover, signed multi-user claim, production claim,
  benchmark, image-quality evaluation, or broad refactor.
- No Local Coding product edit unless direct evidence in this round reveals a
  concrete defect; repository-only acceptance support is allowed.
- No raw prompt/source/image/tool output/body/credential in Git, report, logs,
  metrics, or retained temporary state.

## Verification and acceptance criteria

1. Full architecture wording is cited accurately; simulated compaction is
   classified on the eight fidelity properties above.
2. Focused rehydration/invalidation/isolation/privacy tests pass, and the live
   two-request simulated boundary proves injected hidden-sentinel compliance
   with zero second-request compiler-model attempts.
3. The non-vision ledger has no unresolved product defect or missing evidence;
   otherwise the exact remaining item is reported and vision is not presented
   as the sole gap.
4. The repository-only vision runner and deterministic fixtures pass fake/
   focused tests and are immediately runnable once the protected vision unit is
   active; wheel inspection proves the support is not shipped.
5. The exact human fixture contract, execution command/entry point, PASS facts,
   cleanup, and protected text rollback expectations are documented without
   secrets or protected mutation.
6. `uv lock --check`; frozen sync; Ruff check/format; mypy; focused tests; full
   pytest; build; wheel/sdist boundary inspection; compileall; shell syntax;
   `git diff --check`; secret/raw-content scans all pass.
7. Candidate 18031 and all temporary state are removed. Protected text service,
   Qwen checkout/launch/unit/config, vision unit/config, credentials, profiles,
   network/firewall/VPN, and port 18020 remain unchanged.
8. Same PR only, exact bounded diff, current required GitHub checks green, and
   no merge.

## Completion and stop rule

If simulated-compaction acceptance and every non-vision criterion pass and the
vision runner is ready, report `COMPLETE` for this round while leaving Objective
004 pending solely on human-provided live vision evidence. Do not run or claim
vision. Strategy will then issue exactly:
`HUMAN ACTION REQUIRED — VISION FIXTURE`.

If a genuine non-vision product defect remains, report it precisely and do not
ask for vision prematurely. Do not open or recommend another environment or
compaction diagnostic round.

## Publication contract

Commit/push the exact activated `oap/active`, this immutable order, and the two
prepublication OAP-validator regex edits with the bounded work on the existing
PR branch. Push all non-report work first and record its literal 40-hex SHA.
Publish exactly one immutable
`oap/reports/004-aa-simulated-compaction-and-vision-readiness.md` with literal
implementation SHA and `Report publication commit: SELF`. SELF must be the sole
final commit, change only that report, have the implementation SHA as first
parent, and be remote PR head before response FIFO `OK`.
