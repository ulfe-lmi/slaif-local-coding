# INITIAL STRATEGIC ROADMAP — NON-ACTIVE

Live GitHub/host evidence and prior PR review override this plan. One numeric
objective=one PR; every continuation suffix (`b..z`, then `aa..zz`) amends the same PR. Do not activate next numeric objective
until current accepted, merged, and remote main verified.

| ID | Objective | Required proof | Explicit exclusion |
|---|---|---|---|
| 000 | Live contract, Python/CI foundation, faithful async adapter on 18031, route-scoped newest-image policy | fake upstream + bounded live text/tool/stream/vision/two-image; SSE/errors preserved; 18020/current Codex path unchanged | constitution compiler/cache; gateway integration; cutover |
| 001 | Detect effective AGENTS blocks; deterministic candidate path/evidence extraction; schemas/identity | captured Codex fixtures; false-positive/URL/traversal/nested-path tests; hashes and bounded state | model-generated summary/injection |
| 002 | Direct non-recursive constitutional compiler + strict validation/cache | independent confidence/priority; normative exceptions; invalid JSON/retry/no-pollution; dedup/concurrency/budget | request injection; client filesystem access |
| 003 | Working-set selection, stable injection, dependency acquisition, compaction rehydration | idempotence; every-request reinjection; missing P1 acquisition; invalidation/TTL/LRU/isolation/failure fallback | public gateway/cutover |
| 004 | Real Codex E2E, security, observability, systemd candidate service | long AGENTS sentinel before/after compaction; full image→crop; tool loops; disconnect; raw-log scan; 18031 operations | current-path replacement |
| 005 | SLAIF Gateway contract and explicit rollback-proven cutover | signed identity/route metadata; quota/accounting boundary; candidate soak; backup/switch/rollback; vLLM private | monorepo/copy gateway code |
| 006 | SME package/release | reproducible install/upgrade/rollback; OCI/systemd; pinned provenance/licenses; capacity/runbook; tested-hardware claims | certification/frontier equivalence |

## Activation rule

First candidate order is
`initial-orders/000-a-live-contract-and-adapter-foundation.md`. It is a draft
until strategic Codex independently verifies repository default branch/SHA,
open PRs, actual running services/ports/profiles, credentials mechanism, and
current Codex request shape. Replace every placeholder before atomic publication.

## Cross-repository rule

`slaif-api-gateway` changes, when eventually required, receive their own OAP
workflow/PR in that repository. This roadmap may define the interface but cannot
silently mutate both repositories in one coding PR.
