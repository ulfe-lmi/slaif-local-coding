# OAP Coding-Agent Report — 013-d

## Work order
- Identifier: `013-d`; order path `oap/orders/013-d-corrected-credential-publication-and-peer-freeze.md` (strategic-authored bytes committed verbatim in W3, SHA-256 `0ce557637da0d1edb87353cdb9bdbc1ec4a961e4783abe13cbced24ec38146bf`); `oap/active` → `013-d`; numeric objective `013`
- PR mode: `AMENDED_EXISTING_PR` — PR #15 (the single objective-013 PR), branch `oap/013-mvp-release-publication`; NO new PR

## Status
BLOCKED (at C6 — the single controlled dispatch at W was EXECUTED and FAILED at layer 6, the in-script digest-capture/verification step, AFTER the GITHUB_TOKEN push to the qualified `ghcr.io/…` reference was accepted by the registry (push exit 0). The corrected GITHUB_TOKEN + qualified-reference mechanism is therefore proven engaged and registry-accepted through layer 5; the failure is a deterministic in-repo script defect in `scripts/release_registry_publish.py` digest capture, not an authorization/permission class and not transient infrastructure. Per the order's C6 stop law (partial registry state: tag written, verification not completed) this round STOPs: no re-dispatch, no script fix (behavioral change to that script is an explicit non-goal of this round), no `P`, no recorded `D`, no executed `docker-published`. C1–C5 are complete, pushed, and CI-green at W3 and W4.)

## Executive summary
W3 (C1 restored GITHUB_TOKEN mechanism with `contents: read` + `packages: write`, fail-fast secret step removed, correction record in the workflow header; C2 publish-script docstring corrected; activated 013-d order + active transcript) and W4 (C4 documentation reconciliation including the new authoritative "Gateway compatibility authority (frozen for 0.1.0)" section and the freeze-rule pointers; generator/E3 objective bump to `013-d`; `M_W4` regenerated with `generated_from = W3`, new sdist hash, wheel `H_new` unchanged, peer frozen at `08ca421…`) were implemented exactly per order, pushed to PR #15, and CI at both is fully green (4/4 each). The broad-PAT repository secret `SLAIF_GHCR_TOKEN` (C3) was removed after W4's CI was fully green and before the C6 dispatch (value never recorded). The pre-dispatch anonymous registry probe found both tags `absent`. The single `workflow_dispatch` release run at W (run `35249918197`) passed layers 1–5 with primary log evidence (GITHUB_TOKEN granted `Packages: write`; wheel binding to `H_new`; two-file compose build; `Login Succeeded`; qualified-reference push accepted with exit 0) and FAILED at layer 6 with `PublishError: no digest line in push output for ghcr.io/ulfe-lmi/slaif-local-coding:sha-<W>`: the script's `_push_digest()` scans the captured stdout for a `^digest: sha256:<64-hex>$` line, but the docker CLI emits push progress — including the final digest line — on stderr (and the current CLI's final line is `<tag>: digest: sha256:… size: N`), so the success path, which had never been exercised (013-b and 013-c pushes both failed before completion), raised after the push had already been accepted. Post-failure anonymous probes: `0.1.0` → `absent` (never tagged/pushed) and `sha-<W>` → anonymous resolution DENIED (HTTP 401 on anonymous manifest HEAD) — consistent with the first push having created the package with non-public (private) default visibility; package metadata is not inspectable with the wrapper's credentials (`403: You need at least read:packages scope`), and no visibility change was performed this round. `D` was not captured; the two-tag single-digest identity does not hold (only the `sha-<W>` tag exists, privately). The protected host was never touched (before/after probes identical). The Git tag `v0.1.0` and the GitHub Release do not exist (strategy post-merge acts, as always).

## Authoritative GitHub state
- Repository: `ulfe-lmi/slaif-local-coding`; PR #15 https://github.com/ulfe-lmi/slaif-local-coding/pull/15 — `OPEN`, `MERGEABLE` (`CLEAN`), `autoMergeRequest: null` (merge performed NO)
- Base: `main` @ `a04693e6792df6a8ad4262acfb46336a0f662202` (unchanged, verified at preflight and at report time)
- Starting remote SHA (at order time): `919eafbf2b4a1225079f3b5eda6f5e8d570790ad` (R, the 013-c report commit)
- W3 = `3bbe80ae33f3d9de4c4aa50ff3c5bc4a907853ad` (C1 + C2 + activated 013-d order + `oap/active` → `013-d`)
- W4 = `W` = `be3c78b2016d5d40ce9155df8f94d14525c43d39` (C4 + generator/E3 objective bump + `M_W4`) — the final implementation head and the IMAGE-SOURCE HEAD
- `P`: does NOT exist (C7 NOT RUN — no recorded `D`)
- R: SELF (this report; first parent = `W`)
- Implementation head SHA: be3c78b2016d5d40ce9155df8f94d14525c43d39
- Report publication commit: SELF
- Implementation commits pushed before report: W3, W4 (both pushed; remote branch head = W4 at report time)
- New PR this round: NO; amended existing PR #15: YES; merge performed: NO
- `D`: NOT captured — the registry-verified single digest of BOTH tags does not exist (see registry state below); the pushed image's content digest is not recoverable from the run (the digest line was captured into the script's stderr stream and never emitted)

## Changes and files
**W3** (`3bbe80a…`, 4 files, none an sdist input, none a Docker build-context path):
- `.github/workflows/release-image.yml` (C1, exactly the authorized edits): header comment now states the active publication path is `workflow_dispatch`-only with the registry credential = workflow `GITHUB_TOKEN` declaring `contents: read` + `packages: write` (the documented mechanism for publishing the workflow repository's container package; no long-lived credential referenced or introduced) plus the concise correction note (013-b "permission ceiling" diagnosis retracted on primary log evidence; 013-b failure was the unqualified push reference, fixed in 013-c C1; 013-c secret workaround withdrawn and its broad PAT secret removed as release-security cleanup); `permissions:` restored to `contents: read` + `packages: write`; the 013-c fail-fast secret step REMOVED; login step restored to the 013-b form `echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.repository_owner }}" --password-stdin`; push step env restored to the 013-b form `SLAIF_GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}` (env name is the publish script's token input for registry-API verification). All other steps byte-identical to the 013-b form; still `workflow_dispatch`-only; the qualified `ghcr.io/…` push references come from the unchanged 013-c C1 script. NO `secrets.SLAIF_GHCR_TOKEN` expression remains in the workflow (or anywhere in active code — grep proof in Verification).
- `scripts/release_registry_publish.py` (C2, docstring ONLY): the credential lines now state the registry credential is the workflow `GITHUB_TOKEN` (declared `packages: write`), passed via the `SLAIF_GHCR_TOKEN` environment variable, never printed. NO behavioral change — the qualified-reference logic, no-silent-repoint guard, digest capture, registry verification, and output contract are byte-for-byte as in W2 (only the module docstring lines differ; `--self-check-refs` proof re-ran green).
- `oap/orders/013-d-corrected-credential-publication-and-peer-freeze.md` (activated order, strategic-authored bytes verbatim; SHA-256 `0ce557637da0d1edb87353cdb9bdbc1ec4a961e4783abe13cbced24ec38146bf`)
- `oap/active` → `013-d` (one-line change)

**W4** (`be3c78b…` = `W`, 9 files):
- `docs/RELEASE-ARTIFACT-POLICY.md` (C4): the two publication-statement sites and the canonical Publication section reconciled to the GITHUB_TOKEN mechanism (dispatch-only; declared `packages: write`; no long-lived credential; cites the current GitHub Container-registry authentication guidance); the ceiling-rejection prose replaced by the correction record (the "ceiling" diagnosis was wrong; no repository security setting was ever changed); the 013-c "dedicated fine-grained PAT" hardening advisory removed (unsupported); the CORRECT documented human/CLI option stated (classic PAT with at least `write:packages` — explicitly NOT the broad `repo` scope; org SSO if required; fine-grained PATs not a supported GHCR credential type); the "NOT yet published … R18 hold" prose replaced with the 013-d truth (publication is the `workflow_dispatch`-only release workflow executed at the final implementation head of this PR; digest/tags/`S` recorded in `packaging/release_record.json`, present from the release-record commit onward, and the schema-v3 manifest; path-only reference discipline kept); NEW authoritative section "Gateway compatibility authority (frozen for 0.1.0)" (freeze at `08ca421…`; 18/18 gate evidence; blob-identical contract surface across the three pins; CI fixture = development compatibility-tracking only, never changes the frozen release authority or invalidates 0.1.0; frozen value bound in the Dockerfile ARG default, in-image label, and manifest `gateway_peer.commit`).
- `docs/DOCKER-INSTALL.md` (C4): the publication-mechanism statement reconciled to the GITHUB_TOKEN mechanism ("executed at the final implementation head of this PR (order 013-d)"); the "re-qualified to the current peer" history statement kept as history and aligned with the freeze rule (pointer to the new section).
- `docs/RELEASE-CUTOVER-RUNBOOK.md` (C4): the `gateway.authority_sha` row now names the FROZEN 0.1.0 release compatibility authority `08ca421…` (pointer to the new section) with the 013-b re-pin retained as history.
- `docs/TOPOLOGY.md`, `docs/SLAIF-GATEWAY-INTEGRATION.md`, `docs/IMPLEMENTATION-ROADMAP.md` (C4): one minimal pointer line each at the pin prose (freeze rule: the CI fixture may follow Gateway `main` as development tracking without changing the frozen release authority); historical re-pin statements remain as history.
- `scripts/release_provenance_manifest.py`: `OBJECTIVE` → `"013-d"` + docstring reference (013-d).
- `tests/test_release_provenance_manifest.py`: E3 objective assertion → `"013-d"`.
- `packaging/release_provenance_manifest.json` → `M_W4`: `objective "013-d"`; not-yet-published state (`oci.published: false`, `oci.image_digest: null`, no `release` key, `status.released: false`); `gateway_peer.commit = 08ca421bee1ddca62078302b910e8be88cf705be` (FROZEN release authority — unchanged); `artifacts.wheel.sha256 = oci.wheel_sha256 = 879baa3ad19e0f513090add965d267957d27f7bcd904d1e225586d76126f8b19` (= `H_new`, 83,352 B, 26 entries, unchanged); `artifacts.sdist.sha256 = 846220ba36da57cc91b3b38d93270908d7c8d177df06332dafe4873ed844d580` (NEW — 438,162 B, 107 entries; equals a fresh `uv build` at W4's tree, verified); `generated_from.git_commit = 3bbe80ae33f3d9de4c4aa50ff3c5bc4a907853ad` (= W3).

**No other file changed** in this round (verified: `git diff --name-only 919eafb..W4` = the 13 files above; the pre-existing unrelated worktree items — `oap/runtime.env.example` modification and the empty root files `Local`/`clean`/`unchanged` — are untouched and uncommitted, exactly as found at round start).

## Acceptance evidence
### C1 — PASSED (implementation + run-log proof)
- Restored exactly per the five authorized edits (see Changes); workflow YAML parses; `permissions: {contents: read, packages: write}`; steps: checkout → setup-uv → setup-python → wheel-binding → two-file compose build → `Authenticate to GHCR (GITHUB_TOKEN only)` → `Push tags and registry-verify a single digest`; still `workflow_dispatch`-only.
- Run-log proof at W (run `35249918197`, "Set up job" → GITHUB_TOKEN Permissions): `Contents: read` / `Metadata: read` / `Packages: write` — the declared permissions were honored in THIS round's run, re-confirming the diagnostic correction on fresh primary evidence.
- Grep proof: the ONLY `secrets.` references in the workflow are `secrets.GITHUB_TOKEN` (login step and push-step env).

### C2 — PASSED
- Docstring-only change; all behavioral code byte-identical to W2 (diff shows only the two docstring paragraphs).
- `python3 scripts/release_registry_publish.py --self-check-refs` → `reference self-check OK: sha-tag ghcr.io/ulfe-lmi/slaif-local-coding:sha-0000000000000000000000000000000000000000; release-tag ghcr.io/ulfe-lmi/slaif-local-coding:0.1.0` (fixture git-sha, no host push).

### C3 — PASSED (removed; value never recorded)
- `gh secret remove SLAIF_GHCR_TOKEN --repo ulfe-lmi/slaif-local-coding` executed `2026-09-17T16:58:43Z` — AFTER W4's CI was fully green (run `35249230880` SUCCESS 4/4) and BEFORE the C6 dispatch (run `35249918197` started `2026-09-17T16:59:09Z`), exactly per the order's timing.
- Verification: pre-removal `gh secret list` showed exactly `SLAIF_GHCR_TOKEN  2026-09-17T11:27:42Z` (the 013-c workaround, value never recorded); post-removal `gh secret list` is EMPTY (the name is gone; the repository now has zero secrets).
- The wrapper's own PAT (its `gh` auth) is untouched; no other secret touched; NO repository Actions/security setting modified (the `default_workflow_permissions` setting was never touched by this round; the correction record explains why no change is needed — it is the fallback default, not a ceiling).

### C4 — PASSED
- Exactly the order-named sites updated (RELEASE-ARTIFACT-POLICY L34-46 + L198 + Publication section + ONE new authoritative section; DOCKER-INSTALL ~L24 + ~L326-335; RELEASE-CUTOVER-RUNBOOK ~L54; one-line pointers in TOPOLOGY, SLAIF-GATEWAY-INTEGRATION, IMPLEMENTATION-ROADMAP).
- Content present: GITHUB_TOKEN mechanism (dispatch-only; declared `packages: write`; no long-lived credential; current GitHub Container-registry authentication guidance cited); the correction record (ceiling diagnosis wrong; no repository security setting ever changed; secret workaround withdrawn + removed); the CORRECT human/CLI option (classic PAT with at least `write:packages`, explicitly NOT broad `repo`; org SSO if required; fine-grained PATs not a supported GHCR credential type); the frozen 0.1.0 release compatibility authority section with the CI-fixture-as-development-tracking distinction.
- B5 released-truth discipline intact: path-only digest references (no literal digest in any document); the Git tag `v0.1.0` and the GitHub Release are described as strategy post-merge acts that do not exist (verified: zero Git tags and zero GitHub releases on the repository at report time); publication described as registry-only with cutover NOT performed; no production-ready/certification/generic-equivalence claims.

### C5 — PASSED
- W3 = child of `919eafb…` (4 files; NO generator/test/manifest/doc changes; `oap/` excluded from every artifact; `scripts/` and `release-image.yml` not sdist inputs). Fresh `uv build` at W3's tree reproduced the committed manifest hashes byte-for-byte (wheel `879baa3a…` = `H_new`; sdist `8e0298f0…` = the 013-c manifest sdist hash) — sdist inputs and the committed manifest mutually consistent.
- CI at W3 FULLY GREEN 4/4 — run `35248108211`: `test`, `gateway-contract`, `docker`, `docker-published` all SUCCESS (`docker-published` on the explicit not-yet-published skip path, record absent).
- W4 = child of W3 (9 files). CI at W4 FULLY GREEN 4/4 — run `35249230880`: `test`, `gateway-contract`, `docker`, `docker-published` all SUCCESS (same explicit skip line).
- **No-image-change proof (required):** `git diff --name-only 9c04669a4fcbb65267fc6cd19f0b8176ec33a5e2..be3c78b2016d5d40ce9155df8f94d14525c43d39` = exactly:
  ```text
  .github/workflows/release-image.yml
  docs/DOCKER-INSTALL.md
  docs/IMPLEMENTATION-ROADMAP.md
  docs/RELEASE-ARTIFACT-POLICY.md
  docs/RELEASE-CUTOVER-RUNBOOK.md
  docs/SLAIF-GATEWAY-INTEGRATION.md
  docs/TOPOLOGY.md
  oap/active
  oap/orders/013-d-corrected-credential-publication-and-peer-freeze.md
  oap/reports/013-c-fix-registry-credential-and-complete-publication.md
  packaging/release_provenance_manifest.json
  scripts/release_provenance_manifest.py
  scripts/release_registry_publish.py
  tests/test_release_provenance_manifest.py
  ```
  Build-context intersection (`Dockerfile`, `.dockerignore`, `pyproject.toml`, `uv.lock`, `LICENSE`, `NOTICE`, `README.md`, `src/`): **NONE** (grep over the list returned no match). Therefore the image built at W (runner-built local image `sha256:57f5b0e8802b98bcb918b465915ff712d77996d123e98781032e4aba3acea577`, discarded with the runner) is layer-identical to the W2-built image modulo the per-build `org.opencontainers.image.revision` label (design intent).

### C6 — BLOCKED (the round's blocker: in-script digest-capture defect after a registry-accepted GITHUB_TOKEN push)
- Pre-dispatch anonymous registry probe (token NOT in the environment; `scripts/ghcr_tag_check.py`, `2026-09-17T16:59:00Z`): `0.1.0` → `absent`; `sha-be3c78b2016d5d40ce9155df8f94d14525c43d39` → `absent`.
- Dispatch (EXACTLY ONE): `gh workflow run release-image.yml --ref oap/013-mvp-release-publication` at `2026-09-17T16:59:09Z` with branch tip verified = W (`git ls-remote` before dispatch: `be3c78b2016d5d40ce9155df8f94d14525c43d39 refs/heads/oap/013-mvp-release-publication`). Run **`35249918197`**, headSha `be3c78b2016d5d40ce9155df8f94d14525c43d39`, started `2026-09-17T16:59:13Z`, completed `2026-09-17T16:59:34Z`, `completed/failure`.
- **Layer classification (each layer stated separately, with raw log evidence):**
  1. **workflow start + checkout — SUCCESS**: run started 16:59:13Z; `GITHUB_TOKEN Permissions` block logged; `Run actions/checkout@v4` success.
  2. **wheel-binding assert — SUCCESS**: `fresh wheel sha256:    879baa3ad19e0f513090add965d267957d27f7bcd904d1e225586d76126f8b19` = `manifest wheel sha256: 879baa3ad19e0f513090add965d267957d27f7bcd904d1e225586d76126f8b19` (= `H_new`); `SLAIF_WHEEL_SHA256` exported.
  3. **image build — SUCCESS**: two-file compose build completed; `built image: sha256:57f5b0e8802b98bcb918b465915ff712d77996d123e98781032e4aba3acea577 (push performed in the next steps)` (the `mvp-release-0.1.0` qualification label ARG was set by the step).
  4. **GHCR authentication success — SUCCESS**: `echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "ulfe-lmi" --password-stdin` → `Login Succeeded` (GITHUB_TOKEN; env masked `SLAIF_GHCR_TOKEN: ***` in the push step).
  5. **GHCR authorization + image upload success — SUCCESS at the registry transport/authorization level**: `publishing slaif-local-coding:0.1.0-be3c78b… as ghcr.io/ulfe-lmi/slaif-local-coding:sha-be3c78b…` (qualified reference, 013-c C1 fix active); `docker push` returned exit code 0 (the script's `_docker(check=True)` raises `PublishError: docker push … failed: <stderr>` on any nonzero exit; no such error occurred), with no scope/permission denial — the push ran ~8.4 s (16:59:25.660Z → 16:59:34.037Z), consistent with a real upload of the W-built image.
  6. **in-script registry verification — FAILED (the exact failing layer)**: the script raised
     ```text
     PublishError: no digest line in push output for ghcr.io/ulfe-lmi/slaif-local-coding:sha-be3c78b2016d5d40ce9155df8f94d14525c43d39
     ```
     at `scripts/release_registry_publish.py:62` (`_push_digest`), run exit code 1 (`##[error]Process completed with exit code 1.`). The `0.1.0` tag was NEVER tagged or pushed (the script pushes the sha-tag first and died inside its digest capture). Root cause (deterministic in-repo defect, NOT an authorization/permission class, NOT transient infrastructure): `_push_digest()` scans `proc.stdout` (separately captured) for `^digest:\s*(sha256:[0-9a-f]{64})\s*$`, but the docker CLI writes push progress — including the final digest line — to **stderr**, and the current docker CLI's final line is `<tag>: digest: sha256:<64-hex> size: <n>` (not `digest: …` at line start). This success path had NEVER been exercised: the 013-b pushes (unqualified reference) and the 013-c push (scope denial) all failed before a completed push, so the digest-capture code ran for the first time ever in this run.
  7. **package visibility / anonymous pullability — NOT RUN in-run** (the run died at layer 6 before any in-script verification). Post-failure host-side anonymous probes (below): both tags are NOT anonymously resolvable. The first push created the package (the push was registry-accepted); anonymous access is denied (HTTP 401 on anonymous manifest HEAD) — consistent with a non-public (private) default-visibility package. The intended semantics for this public repository's pull-based MVP is a PUBLICLY pullable package; making it so is a C6 layer-7 act that was never reached, and the package-visibility API is NOT available with the wrapper's credentials (`403: You need at least read:packages scope to get a package.` on `GET /orgs/ulfe-lmi/packages/container/slaif-local-coding`), so per the order it would be a human UI act. **No visibility change was performed this round.**
  8. **capture of `D` — NOT RUN**: no digest captured from the run or verified via the registry; the pushed image's content digest is not recoverable from the run (the digest line lived in the script's captured stderr and was never emitted to the workflow log).
- **Classification per the order's C6 failure law:** the failure is neither an authorization/permission-class failure (layers 4–5 succeeded with the corrected GITHUB_TOKEN mechanism — the exact class 013-c blocked on is DISPROVEN by this run) nor a transient-infrastructure failure (deterministic in-repo script defect). The order permits ONE deterministic re-dispatch ONLY for transient-infrastructure failures, forbids re-dispatch for other classes, forbids a second failure of any kind, and — independently — makes any behavioral change to `scripts/release_registry_publish.py` an explicit non-goal of this round. Combined with the partial registry state (tag written, verification not completed), the order's stop law applies: **STOP and report.** No re-dispatch, no workaround, no orphan cleanup, no repoint (orphan cleanup / visibility / completion are strategic acts).
- **Registry state stated exactly (post-failure anonymous probes, `2026-09-17T17:07:32Z`, `scripts/ghcr_tag_check.py` with `SLAIF_GHCR_TOKEN` NOT in the environment):**
  - `0.1.0` → `absent` (tag never written)
  - `sha-be3c78b2016d5d40ce9155df8f94d14525c43d39` → `absent` as reported by the script's anonymous-exchange flow; raw registry probe: anonymous token exchange → 401, anonymous manifest HEAD → `401 UNAUTHORIZED {"errors":[{"code":"UNAUTHORIZED","message":"authentication required"}]}` (denied, not 404) — consistent with the tag existing in a non-public package. `docker buildx imagetools inspect` (anonymous, manifest-only, no download) failed with the same 401 on the anonymous token fetch.
  - The two-tag single-digest identity `digest(0.1.0) == digest(sha-<W>) == D` does NOT hold; `D` (as a registry-verified, recorded release digest) does not exist.

### C7 — NOT RUN
- No recorded `D` → `packaging/release_record.json` was NOT written and `P` was NOT created. `P` remains defined for the round that actually publishes: child of W, EXACTLY two files (`packaging/release_record.json` per the 013-a R1 field set with `image_source_commit = W`, `oci_image_digest = sha256:<D>`, `oci_tags = ["0.1.0", "sha-<W>"]`, `published_at` RFC 3339 UTC of the successful run, `publication_workflow_run_id` = the successful run id; and `M_P` regenerated from W's tree in the published state, `generated_from.git_commit = W`).

### C8 — NOT RUN
- CI at `P` (including the EXECUTED `docker-published` job) and the `P`-parented report are NOT RUN: no `P`, no recorded `D`, no publicly pullable published image. The `docker-published` job's correct at-W behavior (explicit not-yet-published skip) is verified in the CI section. The C8 narrow gate-defect exception was NOT needed and NOT used (the failure is in the release workflow's publish script, outside the exception's `ci.yml`/`docker_qualification_ci.py` scope).

## Verification
All local gates at W4 (working tree = W4 exactly; CI-equivalent environments as noted):
- `uv run --frozen ruff check .` (repo venv): **PASSED** — `All checks passed!`
- `uv run --frozen ruff format --check .` (repo venv): **PASSED** — `360 files already formatted`
- mypy in the CI-equivalent environment (fresh `UV_PROJECT_ENVIRONMENT` venv, `uv sync --frozen --extra dev` ONLY, mirroring the CI `test` job): `mypy src tests` → **PASSED** — `Success: no issues found in 71 source files`. (The long-lived repo venv additionally carries the `gateway-contract` group (SQLAlchemy), which makes two pre-existing `type: ignore[import-not-found]` comments in `scripts/gateway_accounting_rehearsal.py` report as unused locally — the established 013-c documented local-environment artifact; with the CI-identical dev-only environment they are used and mypy is clean. No file in this round touches that script.)
- `uv run --frozen pytest -q` (repo venv): **PASSED** — `1098 passed, 26 skipped in 116.73s` (the standing 26 routine skips remain as designed; the one extra local pass vs CI is a Gateway-dependent test enabled by the local venv's `gateway-contract` group — CI reports `1097 passed, 27 skipped` in its dev-only environment)
- `python -m compileall -q src tests oap/bin scripts`: **PASSED**
- `bash -n` on changed shell scripts: **NOT NEEDED** — this round changed no shell scripts (the CI `bash -n oap/bin/*.sh packaging/*.sh` gate ran green within both W3/W4 `test` jobs)
- `uv build` (fresh, at W3's tree): **PASSED** — wheel `879baa3ad19e0f513090add965d267957d27f7bcd904d1e225586d76126f8b19` (= `H_new`) and sdist `8e0298f0ba10b2f633bace7d9369cdb866960d65921493ffc0db613944efbd6e` (= committed manifest sdist hash; W3 sdist-consistency proof)
- `uv build` (fresh, at W4's tree): **PASSED** — wheel `879baa3a…` (= `H_new`, unchanged) and sdist `846220ba36da57cc91b3b38d93270908d7c8d177df06332dafe4873ed844d580` (= `M_W4` sdist hash); byte-for-byte reproducible
- `python scripts/artifact_policy_check.py --dist <fresh W4 build> --inspect`: **PASSED** — `ok: true, violations: []`
- `python scripts/artifact_policy_check.py --dist <fresh W4 build> --install-smoke`: **PASSED** — `ok: true, violations: []`
- manifest module suite `pytest -q tests/test_release_provenance_manifest.py` (at W3 with the committed 013-c manifest: 18 passed; at W4 with `M_W4` + objective `013-d`: **PASSED** — `18 passed`)
- gateway-contract local re-run at the FROZEN peer: fresh checkout of `ulfe-lmi/slaif-api-gateway` at `08ca421bee1ddca62078302b910e8be88cf705be` (origin/HEAD verified), `SLAIF_GATEWAY_ROOT=… python scripts/gateway_contract.py --gateway-root … --run-tests`: **PASSED** — `{"checkout_clean": true, "commit": "08ca421…", "network_guard": "enabled", "tests_collected": 18, "tests_errors": 0, "tests_failed": 0, "tests_passed": 18, "tests_skipped": 0}`
- `docker compose -f compose.yaml -f compose.build.yaml config --quiet` and `docker compose -f compose.yaml config --quiet` (temporary 0-byte env/config files, established 013-b pattern): **PASSED** — render only; **NO host docker build/run/up**
- `python3 scripts/release_registry_publish.py --self-check-refs`: **PASSED** (see C2)
- Grep proof — NO `SLAIF_GHCR_TOKEN` repository-secret reference remains in any workflow or active code: the only repository-wide matches for `secrets.SLAIF_GHCR_TOKEN` are in the two immutable OAP transcript files (the 013-c order's quoted workflow text and the 013-d order's own check requirement); zero matches in `.github/`, `scripts/`, `src/`, `tests/`, `docs/`, `packaging/`, `config/`, or the compose files. The remaining `SLAIF_GHCR_TOKEN` occurrences in active code are the publish-script/`ghcr_tag_check` environment-variable name and its docstrings (expected and fine per the order); the workflow sources the env from `secrets.GITHUB_TOKEN` only.
- Pre-dispatch and post-failure anonymous registry probes (`ghcr_tag_check.py`): **PASSED** as probes (exact outputs in C6 — both tags `absent` at the pre-dispatch moment; post-failure `0.1.0` `absent`, `sha-<W>` anonymous resolution DENIED 401)
- Protected-model live matrix (text/tool/SSE/vision/…): **NOT RUN** — explicit non-goal of this order (no protected-model/Qwen inference anywhere; fake/synthetic upstreams only)
- Real Codex E2E: **NOT RUN** — not ordered this round

## Live model/service evidence
- No live model calls were made this round (order non-goal: NO protected-model/Qwen inference anywhere).
- Registry (anonymous, read-only) via `scripts/ghcr_tag_check.py` + raw registry v2 probes:
  - pre-dispatch (2026-09-17T16:59:00Z): `0.1.0` → `absent`; `sha-be3c78b…` → `absent`
  - post-failure (2026-09-17T17:07:32Z): `0.1.0` → `absent`; `sha-be3c78b…` → `absent` (script) / 401 UNAUTHORIZED on anonymous manifest HEAD (raw probe); `docker buildx imagetools inspect` (anonymous) → 401 on anonymous token fetch
  - package metadata API (`GET /orgs/ulfe-lmi/packages/container/slaif-local-coding` with the wrapper's PAT): `403 You need at least read:packages scope to get a package.` — visibility/metadata NOT inspectable with this round's credentials; NO visibility change performed
- Protected-host invariance (read-only probes only; no protected resource was touched; NO docker build/run/up on the host; sudo used only for read-only `docker ps`/`docker images`):

| Item | Before (2026-09-17T16:37:19Z) | After (2026-09-17T17:10:09Z) |
|---|---|---|
| `qwen-serving-vision.service` | active, MainPID=23961 | active, MainPID=23961 |
| `qwen-serving.service` | inactive | inactive |
| Port 18020 | `LISTEN 0.0.0.0:18020`, owner `vllm pid=23961` only | identical |
| Ports 18031/18033/18034 | closed | closed |
| `docker ps -a` | three pre-existing exited non-slaif containers (`0e3109680183`/`cc5e7d551c0a` — `chrockey/fpt-votenet:v0.1.0`, Exited 2 years ago; `379dcff9d7f2` — `hello-world`, Exited 2 years ago) | identical (same IDs/images/statuses); zero slaif images on the host (before and after) |
| `~/.codex/qwen-neumann.config.toml` | size=858, mode=600, mtime=2026-09-13 11:59:21.852183775 +0200 | identical |

## GitHub CI / required checks
- CI at W3 (`3bbe80a…`): run **35248108211** — `test`, `gateway-contract`, `docker`, `docker-published`: **4/4 SUCCESS** (`docker-published` explicit not-yet-published skip, record absent)
- CI at W4 (`be3c78b…` = W): run **35249230880** — `test`, `gateway-contract`, `docker`, `docker-published`: **4/4 SUCCESS** (explicit not-yet-published skip line)
- CI at P: N/A — `P` does not exist this round
- Release-workflow run at W: **35249918197** — `completed/failure` at layer 6 (in-script digest capture; exact error and raw lines in C6); layers 1–5 all SUCCESS with primary log evidence
- All required green at drafting: YES for all CI that could run at this round's final non-report commit (W4); the release publication run itself is the blocker (see C6)
- Report-head checks (triggered by this report push) may be pending at publication; strategy verifies

## Local setup/dependencies
- Repository-owned venv via `uv` (locked/frozen installs); no new dependencies; no changes to `pyproject.toml`/`uv.lock` (frozen by this order).
- The repository venv additionally holds the `gateway-contract` dev group (used for the local gateway-contract gate and manifest-suite runs; see the mypy note in Verification).
- Passwordless sudo used only for read-only `docker ps`/`docker images` probes (established pattern). No terminal operator recruited.
- Disposable `/tmp` scratch only (outside the repository): fresh `uv build` output directories, the fresh dev-only CI-mirror mypy venv, the fresh gateway checkout at `08ca421…`, the 0-byte compose render temp files, and the captured run log.
- One repository-state mutation authorized and performed this round: REMOVAL of the `SLAIF_GHCR_TOKEN` repository secret (C3; name + creation/removal timestamps only; value never recorded). The repository now has zero secrets. No new credential of any kind was created; the ONLY credential act in the C6 run was the per-run workflow GITHUB_TOKEN.

## Documentation
- Updated exactly per C4 (the mechanism sites → GITHUB_TOKEN mechanism with the correction record; the correct human/CLI PAT guidance with the unsupported fine-grained-PAT advisory removed; the "NOT yet published … R18 hold" prose → the 013-d truth; the new authoritative "Gateway compatibility authority (frozen for 0.1.0)" section; the freeze-rule pointers in the four remaining docs). All other B5 released-truth wording intact and still true at this round's completion (no literal digest in any document; the tag/Release objects do not exist and no document claims they do; publication described as registry-only with cutover NOT performed).
- No other documentation changes. `src/`, `pyproject.toml`, `uv.lock`, `config/`, `Dockerfile`, `.dockerignore`, `compose.yaml`, `compose.build.yaml`, the docker-qualification harness, and `ci.yml` are byte-identical across this round (verified by the W2..W4 diff above; the C8 narrow gate-defect exception was NOT used).

## Diagnostic-correction record (re-affirmed with this round's fresh primary evidence)
- 013-b runs `35208313536` (2026-09-17T10:01:55Z) and `35209542430` (2026-09-17T10:15:43Z): "Set up job" → `GITHUB_TOKEN Permissions`: `Contents: read` / `Metadata: read` / `Packages: write` (both runs; strategy re-read both raw logs at 013-d order time). The declared `packages: write` was HONORED, not clamped.
- The 013-b failure was a misdirected (unqualified) push: `docker push ulfe-lmi/slaif-local-coding:sha-dfa77fb442a82754bb6af21a2e0fb0af29ddfbab` (unqualified reference resolves against docker.io — strategy's host probe printed the `The push refers to repository [docker.io/ulfe-lmi/slaif-local-coding]` banner and was rejected), failing pre-transfer with `unauthorized: access token has insufficient scopes`. That error string is attributed to the misdirected push, not to any GHCR decision on a `packages: write` GITHUB_TOKEN.
- The 013-c failure stands as a separate, valid fact: run `35215843816`, qualified `ghcr.io/…` push with the classic PAT (scopes `read:org, repo, workflow` — NO `write:packages`) → `denied: permission_denied: The token provided does not match expected scopes.`
- Current authoritative GitHub documentation (Container registry, "Authenticating to the Container registry"; cited in the 013-d order, verified by strategy at order time): for workflows publishing the workflow repository's packages the supported/recommended mechanism is the workflow GITHUB_TOKEN ("this registry supports granular permissions … we highly recommend you update your workflow to use the GITHUB_TOKEN"); classic PATs require the `write:packages` scope (the broad `repo` scope is explicitly "unnecessary and broad … we recommend you avoid using for GitHub Actions workflows"); fine-grained PATs are NOT a supported GHCR credential type ("GitHub Packages only supports authentication using a personal access token (classic)").
- **Fresh 013-d confirmation (this round, run `35249918197`):** the dispatched run's "Set up job" logged `GITHUB_TOKEN Permissions: Contents: read / Metadata: read / Packages: write`; `docker login ghcr.io` with the GITHUB_TOKEN returned `Login Succeeded`; the qualified-reference push was ACCEPTED by the registry (exit 0, no scope/permission denial). The corrected mechanism — GITHUB_TOKEN with declared `packages: write` + fully qualified `ghcr.io/…` references — is proven to work end-to-end through the registry authorization/upload boundary.

## Gateway freeze record (normative, effective for release 0.1.0)
- Drift at 013-d order time: Gateway `main` at `2b61312e0eb569aa7c6f953f52e35b44e84b91c1` = **16 commits** beyond the frozen pin `08ca421…` (verified this round: `git rev-list --count 08ca421…..2b61312…` = 16).
- Changed file set for that drift (verified this round, `git diff --name-only`): `app/slaif_gateway/main.py`, `app/slaif_gateway/metrics.py`, `docker-compose.production.yml`, `docker-compose.yml`, `docs/observability.md`, `docs/openai-compatibility.md`, three `docs/verification/…` files, `oap/active` + four `oap/orders/…` + four `oap/reports/…` (Gateway-side transcripts), `pyproject.toml`, `tests/unit/test_metrics_multiprocess.py`. **NONE of the three Local contract files.**
- Contract surface blob-identity (verified this round via `git ls-tree` at BOTH SHAs of the fresh gateway checkout):
  - `app/slaif_gateway/modules/servers/local_coding/contract.py` = `9e8ae5ef8e1f9d8d48bf3627b9a767d86593a6ac` (equal at `08ca421…` AND `2b61312…`)
  - `app/slaif_gateway/modules/clients/codex_0149.py` = `8976c984c4430d65b3d36bad8565062a8c6f955a` (equal at both)
  - `app/slaif_gateway/providers/streaming.py` = `73cc9f99e8c3cb2f3eb95173344bfbdc211c71ff` (equal at both)
- Strict contract gate at the frozen peer (local re-run this round, network guard enabled): **18/18 PASSED** (`tests_collected: 18, tests_passed: 18, tests_failed: 0, tests_skipped: 0, network_guard: "enabled"`); the CI authority for the same gate is run `35208071525` (cited in the order) plus the 013-c local re-run.
- The frozen value is bound in `M_W4` (`gateway_peer.commit`), the Dockerfile `SLAIF_GATEWAY_PEER_SHA` ARG default, and the in-image `slaif-local-coding.gateway.peer.sha` label. The CI fixture (`tests/fixtures/gateway/current_peer_authority.json`, `purpose: current_ci_compatibility_authority`, commit `08ca421…`) and `scripts/gateway_contract.py` (`PURPOSE = "current_ci_compatibility_authority"`) were NOT changed by this round and were NOT re-pinned to `2b61312…` (explicit non-goal).

## 013-a R-criteria re-affirmation at W (013-d; P does not exist)
- **R1** (release record exists only in P's tree): **NOT SATISFIED** — no `P`, no recorded `D`; `packaging/release_record.json` is absent from W3/W4 trees as required for the pre-publication state.
- **R2/R3/R4** (schema-v3 state-aware manifest, generator, E3 gate): **SATISFIED at W** — `M_W4` in not-yet-published state (`published: false`, `image_digest: null`, `released: false`, no `release` key, reserved-reference tag convention), `objective "013-d"`, `generated_from = W3` (ancestor of W4); E3 suite 18/18 local and in CI at W3/W4.
- **R5** (byte-identity law): **SATISFIED** — no changes to `src/`, `pyproject.toml`, `uv.lock` in this round (W2..W4 diff); the CI wheel-binding step passed at W3 and W4 (`docker` job) and in the C6 run (`fresh wheel sha256 == manifest wheel sha256 == 879baa3a…`).
- **R6–R10** (label ARG, compose split, harness, static gate): **SATISFIED (unchanged since 013-a)** — `Dockerfile`/`compose.yaml`/`compose.build.yaml`/harness byte-identical across this round; CI `docker` job (full qualification of the locally built image via two-file compose) green at W3 and W4; local `docker compose config` renders green (no build/run).
- **R11** (active workflow, dispatch-only, GITHUB_TOKEN only, no repository secrets, exact step set): **SATISFIED as the CORRECTED mechanism** — the workflow at W3 restores `contents: read` + `packages: write` GITHUB_TOKEN (the documented mechanism; no long-lived credential; the 013-c secret workaround withdrawn and its secret removed in C3), keeps `workflow_dispatch` as the only trigger, and runs the exact required step sequence (proven by run `35249918197`: wheel binding, two-file compose build with the `mvp-release-0.1.0` label, GITHUB_TOKEN login, qualified `ghcr.io/…` push).
- **R12** (actually trigger at S, wait for SUCCESS, capture D + run id into the record at P): **NOT SATISFIED** — the single dispatch at W was executed (run `35249918197`, headSha W) and FAILED at layer 6 (in-script digest capture; the GITHUB_TOKEN push itself was registry-accepted); no `D`, no record. The trigger capability itself is PROVEN (the dispatch ran to the push step).
- **R13** (`docker-published` gate): the job ran at W3/W4 on the explicit not-yet-published skip path (correct pre-publication behavior); the **EXECUTED** published-image gate is **NOT RUN** (no `P`/no publicly pullable published image).
- **R14** (all existing gates unchanged and green): **SATISFIED at W** — `test`, `gateway-contract` (frozen peer `08ca421…`), `docker` all green at W3 and W4 (4/4 each); no 011/012/013 qualification assertion removed, skipped, or weakened.
- **R15/R16** (docs reconciled with post-publication truth): **SATISFIED by the C4 wording** — the publication mechanism, the correction record, the correct PAT guidance, and the frozen 0.1.0 release compatibility authority (with the CI-fixture-as-development-tracking distinction) are documented; the stale secret-mechanism/ceiling/fine-grained-PAT/"R18 hold" prose is gone; all B5 discipline intact (path-only digest references; tag/Release as strategy post-merge acts that do not exist; registry-only; no production-ready claims).
- **R17** (manifest/documentation status agreement): **SATISFIED at W** — `M_W4` states not-yet-published (`released: false`, `published: false`, digest null) and every current-facing document agrees at the W4 state (publication described as the dispatch-only workflow executed at the final implementation head of this PR; the record is "present from the release-record commit onward"). `M_P` (published state) does not exist this round.
- **R18** (gateway-main re-verification before publication): **SUPERSEDED by the 013-d freeze rule** — 013-b re-qualified the peer at `08ca421…`; 013-d freezes that as the 0.1.0 release compatibility authority and diagnoses the `2b61312…` drift (16 commits, no contract-file change, blob-identity verified this round). The freeze rule is the normative successor for 0.1.0.
- **R19** (exact round sequence): **TRUNCATED at C6** — implementation commits → W (M_W4) → CI at W fully green (docker-published explicit skip) → single dispatch at W → workflow FAILURE at layer 6 → STOP per the C6 stop law. No `P`, no `R`-beyond-facts; coding never merges, never creates a Git tag, never creates a GitHub Release.
- **R20** (protected-host invariance): **SATISFIED** — before/after read-only probes identical (table above); NO docker build/run/up on the host.

## Safety/scope confirmations
- Unrelated files preserved: pre-existing worktree items (`oap/runtime.env.example` modification; empty root files `Local`, `clean`, `unchanged`) untouched and uncommitted; no reset/clean/overwrite performed.
- Secrets/raw content: the removed secret's value never entered any report, commit, log, or command line (it was created in 013-c via pipe and removed by name only); the workflow masks `secrets.GITHUB_TOKEN` references; the script never prints the token; no prompts/source/images/tool outputs/bodies in any artifact; protected-host paths cited only as established conventions.
- Production/protected resources: **Protected 18020/Qwen/Codex fixture changed: NO** (before/after probes identical; no systemd/network/service/firewall/VPN mutation; no host docker build/run/up).
- NO Gateway-repository mutation (read-only fresh checkout for the local contract gate and the freeze-record git queries; peer remains `08ca421…`; the CI fixture was NOT re-pinned). NO Git tag and NO GitHub Release by coding (both remain strategy post-merge acts; verified zero of each at report time). NO modification of any repository Actions/security setting, in any direction (the `default_workflow_permissions` setting is NOT touched; the correction record explains why no change is needed).
- NO creation of new credentials. The ONLY credential acts this round: use of the per-run workflow GITHUB_TOKEN (C6), and REMOVAL of the `SLAIF_GHCR_TOKEN` secret (C3). The C6 package-visibility act was NOT available with the wrapper's credentials (no `read:packages`; 403) and was NOT performed — it remains a human UI act if/when strategy re-adjudicates.
- NO version change (stays `0.1.0`). NO changes to `src/`, `pyproject.toml`, `uv.lock`, `config/`, `Dockerfile`, `.dockerignore`, `compose.yaml`, `compose.build.yaml`, the docker-qualification harness, or `ci.yml` (the C8 narrow gate-defect exception was NOT used). NO behavioral change to `scripts/release_registry_publish.py` (C2 is docstring-only).
- NO registry publication other than the single registry-accepted push of the `sha-<W>` tag of the one image built at W (the `0.1.0` tag was never written; no repoints; no other tags). No image rebuild after the run.
- Required tests skipped/not run: the protected-model live matrix and real Codex E2E are NOT RUN by explicit order non-goal; the `docker-published` EXECUTED gate is NOT RUN (no `P`/no published image) — all stated as such, none claimed passed.
- Scope deviation: none.
- Extra objective PR: NO. Coding merge: NO. Auto-merge: none requested or present.
- Active/order edited: NO (strategic-authored order bytes committed verbatim; SHA-256 recorded above). Report commit report-only: will be verified at publication (only `oap/reports/013-d-corrected-credential-publication-and-peer-freeze.md` staged).
- Worktree residue: untouched (as above).

## Known limitations/blockers
- **The blocker (exact):** the C6 run failed at layer 6 with `PublishError: no digest line in push output for ghcr.io/ulfe-lmi/slaif-local-coding:sha-be3c78b2016d5d40ce9155df8f94d14525c43d39` — a deterministic defect in `scripts/release_registry_publish.py::_push_digest()` (digest line sought in captured stdout with a `^digest: …$` regex, while the docker CLI emits the final push line on stderr in the `<tag>: digest: sha256:… size: N` form). The success path had never been exercised before this run. This is NOT an authorization/permission boundary (layers 4–5 succeeded: `Packages: write` granted, `Login Succeeded`, push accepted exit 0) and NOT transient infrastructure; the order's C6 stop law (partial registry state: tag written, verification not completed) and the round's explicit non-goal (no behavioral change to the publish script) jointly prohibit fixing or re-dispatching in this round.
- **Registry state (exact):** package `ulfe-lmi/slaif-local-coding` (container type) exists in a non-public state — anonymous resolution of `sha-be3c78b…` is denied (401), `0.1.0` was never written. The pushed image (the W-built image, runner-local ID `sha256:57f5b0e8802b…`, discarded with the runner) is reachable only with registry credentials. Its content digest is not recoverable from the run. No repoint was performed; orphan cleanup, the visibility change to public (intended semantics for this public repository's pull-based MVP), and the completion of the publication (fixed script + single re-dispatch + `0.1.0` tag + record `P`) are strategic/human acts.
- The `SLAIF_GHCR_TOKEN` secret is REMOVED (2026-09-17T16:58:43Z); the repository has zero secrets. If strategy re-adjudicates toward the corrected GITHUB_TOKEN mechanism, no secret re-provisioning is needed (the workflow uses the per-run GITHUB_TOKEN).
- The wrapper's PAT lacks `read:packages` (403 on package metadata/visibility APIs) — the C6 layer-7 visibility verification/change cannot be performed with this round's credentials; it would be a human UI act (or a human-granted scope) per the order.
- `D` is unknown; the two-tag single-digest identity does not hold; the release record and `M_P` (published state) do not exist.

## Recommended strategic follow-up
Factual only; strategy decides:
1. The corrected GITHUB_TOKEN mechanism is PROVEN through the registry authorization/upload boundary (run `35249918197` layers 1–5). The remaining defect is a two-line-class fix in `scripts/release_registry_publish.py::_push_digest()` (read the push's stderr; match the current CLI's `<tag>: digest: sha256:<64-hex> size: N` final-line form) — a behavioral change the 013-d order excludes from this round. With a successor order (or explicit re-adjudication) authorizing that minimal fix, the completion path is mechanical: fix at a new W' → CI 4/4 → the single dispatch → both tags → `D` → `P` (record + `M_P`) → CI at P with the EXECUTED `docker-published` → report.
2. The partial registry state (private package holding only the `sha-<W>` tag) requires a strategic decision: orphan cleanup (delete the package/version) vs. retention for the successor dispatch (the same image is the W-built image; a re-dispatch at the same W after the script fix would re-push the identical content and complete the two-tag publication). No silent repoint was performed.
3. The C6 layer-7 visibility act (package → public, per the intended pull-based MVP semantics) is a human UI act with the current credential set (403 without `read:packages`); alternatively a human-granted `read:packages`/`write:packages` scope or the org-admin UI.
4. PR #15 remains the single objective-013 PR, `OPEN`/`MERGEABLE` (`CLEAN`), head = W4, base = `main` @ `a04693e…`; the 013-c blocker (token-scope denial) is SUPERSEDED by this round's evidence (the denial class did not recur with the GITHUB_TOKEN).
